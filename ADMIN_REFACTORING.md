# План рефакторинга webapp/js/admin.js

## Статус рефакторинга

**Текущий размер файла:** ~1930 строк  
**Количество функций:** ~16  
**Целевой размер:** ~100–200 строк (только инициализация, экспорты и импорты)  
**Планируется выгрузить:** ~1700+ строк (утилиты, обработчики, UI компоненты)  
**Прогресс:** 🔄 В процессе (STEP 1.1: showNotification; STEP 1.2: getCurrentShopSettings; STEP 1.3: loadShopSettings - все выгружены, ожидают проверки)

---

## Список функций для выгрузки из webapp/js/admin.js

### 🔄 1. Модуль "webapp/js/utils/admin_utils.js" (утилиты) - **В ПРОЦЕССЕ**

- 🔄 `showNotification(message)` — показ уведомления (~28 строк) - **В ПРОЦЕССЕ**
  - Используется в: `handleQuantityEnabledToggle()`, `handleReservationsToggle()`, `handleAllProductsMadeToOrderToggle()`, `loadOrders()`, `loadPurchases()`
  - Приоритет: 🟡 СРЕДНИЙ (используется в нескольких местах, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/utils/admin_utils.js`, старый код закомментирован в `admin.js`, ожидает проверки

- 🔄 `getCurrentShopSettings()` — получение текущих настроек (~3 строки) - **В ПРОЦЕССЕ**
  - Используется в: может использоваться в других модулях
  - Приоритет: 🟢 НИЗКИЙ (простая функция-геттер, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/utils/admin_utils.js`, обернута в `admin.js` для доступа к shopSettings, старый код закомментирован, ожидает проверки

- 🔄 `loadShopSettings(shopOwnerId)` — загрузка настроек магазина (~11 строк) - **В ПРОЦЕССЕ**
  - Используется в: может использоваться в других модулях (импортируется в app.js)
  - Приоритет: 🟢 НИЗКИЙ (простая функция загрузки, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/utils/admin_utils.js`, обернута в `admin.js` для доступа к shopSettings, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ** (showNotification, getCurrentShopSettings и loadShopSettings выгружены, ожидают проверки)

**Приоритет:** 🟢 НИЗКИЙ (простые утилиты, низкий риск - можно делать рано)

---

### 🔄 2. Модуль "webapp/js/handlers/admin_init.js" (инициализация и UI) - **В ПРОЦЕССЕ**

- 🔄 `initAdmin()` — инициализация админки (~70 строк) - **В ПРОЦЕССЕ**
  - Используется в: вызывается при загрузке модуля
  - Приоритет: 🟡 СРЕДНИЙ (критичная функция инициализации, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_init.js`, обернута в `admin.js` для передачи зависимостей, старый код закомментирован, ожидает проверки

- 🔄 `createAdminModal()` — создание модального окна админки (~98 строк) - **В ПРОЦЕССЕ**
  - Используется в: `initAdmin()`
  - Приоритет: 🟡 СРЕДНИЙ (создание DOM элементов, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_init.js`, импортирована в `admin.js`, старый код закомментирован, ожидает проверки

- 🔄 `openAdmin()` — открытие админки (~49 строк) - **В ПРОЦЕССЕ**
  - Используется в: вызывается из других модулей
  - Приоритет: 🟡 СРЕДНИЙ (критичная функция открытия, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_init.js`, обернута в `admin.js` для передачи зависимостей, старый код закомментирован, ожидает проверки

- 🔄 `switchAdminTab(tabName)` — переключение вкладок админки (~40 строк) - **В ПРОЦЕССЕ**
  - Используется в: `initAdmin()`, обработчики кликов на вкладки
  - Приоритет: 🟢 НИЗКИЙ (простая функция переключения, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_init.js`, обернута в `admin.js` для передачи зависимостей, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ** (initAdmin, createAdminModal, openAdmin и switchAdminTab выгружены, ожидают проверки)

**Приоритет:** 🟡 СРЕДНИЙ (функции инициализации, средний риск)

---

### 🔄 3. Модуль "webapp/js/handlers/admin_settings.js" (настройки магазина) - **В ПРОЦЕССЕ**

- 🔄 `handleQuantityEnabledToggle(enabled)` — обработка переключателя количества товаров (~46 строк) - **В ПРОЦЕССЕ**
  - Используется в: обработчик события `quantityEnabledToggle.onchange`
  - Приоритет: 🟡 СРЕДНИЙ (изменение настроек магазина, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_settings.js`, обернута в `admin.js` для передачи зависимостей, старый код закомментирован, ожидает проверки

- 🔄 `handleReservationsToggle(enabled)` — обработка переключателя резервации (~34 строки) - **В ПРОЦЕССЕ**
  - Используется в: обработчик события `reservationsToggle.onchange`
  - Приоритет: 🟡 СРЕДНИЙ (изменение настроек магазина, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_settings.js`, обернута в `admin.js` для передачи зависимостей (включая updateProductsUI), старый код закомментирован, ожидает проверки

- 🔄 `checkAllProductsMadeToOrder()` — проверка состояния товаров (все ли под заказ) (~41 строка) - **В ПРОЦЕССЕ**
  - Используется в: `openAdmin()`
  - Приоритет: 🟢 НИЗКИЙ (проверка состояния, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_settings.js`, используется напрямую в `admin.js` (не требует зависимостей), старый код закомментирован, ожидает проверки

- 🔄 `handleAllProductsMadeToOrderToggle(enabled)` — обработка переключателя "все товары под заказ" (~31 строка) - **В ПРОЦЕССЕ**
  - Используется в: обработчик события `allProductsMadeToOrderToggle.onchange`
  - Приоритет: 🟡 СРЕДНИЙ (массовое изменение товаров, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_settings.js`, обернута в `admin.js` для передачи зависимостей, старый код закомментирован, ожидает проверки

- 🔄 `updateProductsUI(reservationsEnabled)` — обновление UI товаров в зависимости от настройки резервации (~19 строк) - **В ПРОЦЕССЕ**
  - Используется в: `handleReservationsToggle()`
  - Приоритет: 🟢 НИЗКИЙ (обновление UI, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/admin_settings.js`, используется напрямую в `handleReservationsToggle` (не требует зависимостей), старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ** (handleQuantityEnabledToggle, handleReservationsToggle, checkAllProductsMadeToOrder, handleAllProductsMadeToOrderToggle и updateProductsUI выгружены, ожидают проверки)

**Приоритет:** 🟡 СРЕДНИЙ (настройки магазина, средний риск - делать по одной функции)

---

### ⏳ 4. Модуль "webapp/js/handlers/admin_orders.js" (заказы) - **НЕ НАЧАТО**

- ⏳ `loadOrders()` — загрузка и отображение заказов (~488 строк) - **НЕ НАЧАТО**
  - Используется в: `switchAdminTab('orders')`
  - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга, высокий риск - делать очень осторожно!)
  - Дата начала: -
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🔴 ВЫСОКИЙ (очень большая функция с DOM манипуляциями, высокий риск)

---

### ⏳ 5. Модуль "webapp/js/handlers/admin_sold.js" (проданные товары) - **НЕ НАЧАТО**

- ⏳ `loadSoldProducts()` — загрузка и отображение проданных товаров (~296 строк) - **НЕ НАЧАТО**
  - Используется в: `switchAdminTab('sold')`
  - Приоритет: 🟡 СРЕДНИЙ (большая функция, сложная логика рендеринга, средний риск)
  - Дата начала: -
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (большая функция с DOM манипуляциями, средний риск)

---

### ⏳ 6. Модуль "webapp/js/handlers/admin_stats.js" (статистика) - **НЕ НАЧАТО**

- ⏳ `loadStats()` — загрузка и отображение статистики (~169 строк) - **НЕ НАЧАТО**
  - Используется в: `switchAdminTab('stats')`
  - Приоритет: 🟡 СРЕДНИЙ (большая функция, сложная логика рендеринга, средний риск)
  - Дата начала: -
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (большая функция с DOM манипуляциями, средний риск)

---

### ⏳ 7. Модуль "webapp/js/handlers/admin_purchases.js" (покупки) - **НЕ НАЧАТО**

- ⏳ `loadPurchases()` — загрузка и отображение заявок на покупку (~476 строк) - **НЕ НАЧАТО**
  - Используется в: `switchAdminTab('purchases')`
  - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга с изображениями и видео, высокий риск - делать очень осторожно!)
  - Дата начала: -
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🔴 ВЫСОКИЙ (очень большая функция с DOM манипуляциями и медиа контентом, высокий риск)

---

## Итого:

- **Всего функций:** ~16
- **Приблизительный объем кода для выгрузки:** ~1700+ строк
- **Текущий размер файла:** ~1930 строк
- **После удаления закомментированного кода останется:** ~100–200 строк (инициализация, экспорты, импорты)
- **Созданные модули:**
  - ⏳ `webapp/js/utils/admin_utils.js` - утилиты
  - 🔄 `webapp/js/handlers/admin_init.js` - инициализация и UI (STEP 2.1: initAdmin, STEP 2.2: createAdminModal, STEP 2.3: openAdmin, STEP 2.4: switchAdminTab выгружены, ожидают проверки)
  - 🔄 `webapp/js/handlers/admin_settings.js` - настройки магазина (STEP 3.1: handleQuantityEnabledToggle, STEP 3.2: handleReservationsToggle, STEP 3.3: checkAllProductsMadeToOrder, STEP 3.4: handleAllProductsMadeToOrderToggle и STEP 3.5: updateProductsUI выгружены, ожидают проверки)
  - ⏳ `webapp/js/handlers/admin_orders.js` - заказы
  - ⏳ `webapp/js/handlers/admin_sold.js` - проданные товары
  - ⏳ `webapp/js/handlers/admin_stats.js` - статистика
  - ⏳ `webapp/js/handlers/admin_purchases.js` - покупки

---

## Рекомендуемый порядок выгрузки:

1. 🔄 **webapp/js/utils/admin_utils.js** - В ПРОЦЕССЕ (STEP 1) - Базовые утилиты
   - Приоритет: 🟢 НИЗКИЙ (простые утилиты, низкий риск - можно делать рано)
   - Статус: STEP 1.1 (showNotification), STEP 1.2 (getCurrentShopSettings) и STEP 1.3 (loadShopSettings) выгружены, ожидают проверки

2. 🔄 **webapp/js/handlers/admin_init.js** - В ПРОЦЕССЕ (STEP 2) - Инициализация и UI
   - Приоритет: 🟡 СРЕДНИЙ (функции инициализации, средний риск)
   - Статус: STEP 2.1 (initAdmin), STEP 2.2 (createAdminModal), STEP 2.3 (openAdmin) и STEP 2.4 (switchAdminTab) выгружены, ожидают проверки

3. 🔄 **webapp/js/handlers/admin_settings.js** - В ПРОЦЕССЕ (STEP 3) - Настройки магазина
   - Приоритет: 🟡 СРЕДНИЙ (настройки магазина, средний риск - делать по одной функции)
   - Статус: STEP 3.1 (handleQuantityEnabledToggle), STEP 3.2 (handleReservationsToggle), STEP 3.3 (checkAllProductsMadeToOrder), STEP 3.4 (handleAllProductsMadeToOrderToggle) и STEP 3.5 (updateProductsUI) выгружены, ожидают проверки

4. ⏳ **webapp/js/handlers/admin_stats.js** - НЕ НАЧАТО (STEP 4) - Статистика
   - Приоритет: 🟡 СРЕДНИЙ (большая функция с DOM манипуляциями, средний риск)
   - Статус: Ожидает выполнения

5. ⏳ **webapp/js/handlers/admin_sold.js** - НЕ НАЧАТО (STEP 5) - Проданные товары
   - Приоритет: 🟡 СРЕДНИЙ (большая функция с DOM манипуляциями, средний риск)
   - Статус: Ожидает выполнения

6. ⏳ **webapp/js/handlers/admin_orders.js** - НЕ НАЧАТО (STEP 6) - Заказы
   - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга, высокий риск - делать очень осторожно!)
   - Статус: Ожидает выполнения

7. ⏳ **webapp/js/handlers/admin_purchases.js** - НЕ НАЧАТО (STEP 7) - Покупки
   - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга с изображениями и видео, высокий риск - делать очень осторожно!)
   - Статус: Ожидает выполнения

---

## Легенда статусов:

- ✅ **ВЫПОЛНЕНО** - Функция выгружена, протестирована, старый код удален
- 🔄 **В ПРОЦЕССЕ** - Частично выполнено, старый код закомментирован
- ⏳ **НЕ НАЧАТО** - Ожидает выполнения

---

## Примечания:

- Рефакторинг проводится мягко и постепенно, по одной функции
- Старый код сначала закомментирован для проверки работоспособности
- После подтверждения работоспособности (через 1-2 дня) старый код удаляется
- Каждый шаг тестируется отдельно перед переходом к следующему
- Критичные функции (loadOrders, loadPurchases) рефакторятся осторожно
- Функции с DOM манипуляциями требуют особого внимания
- Тесты в браузере не делаем - инициализация проходит через Telegram ID
- Все необходимые логи только в терминалах (console.log)

---

## Шаблон для каждого шага:

### Перед началом:
```javascript
// TODO: REFACTORING STEP X.Y - [Название шага]
// Дата начала: YYYY-MM-DD
// Статус: В процессе / Завершен / Откат
```

### В процессе рефакторинга:
```javascript
// ========== REFACTORING STEP X.Y: [Название] ==========
// НОВЫЙ КОД (используется сейчас)
import { newFunction } from './handlers/new_module.js';
const result = await newFunction(params);

// СТАРЫЙ КОД (закомментирован, будет удален после проверки)
/*
async function oldFunction(params) {
    // старая реализация
}
*/
// ========== END REFACTORING STEP X.Y ==========
```

### После проверки (через 1-2 дня):
- Если все работает: удалить закомментированный код
- Если есть проблемы: откатить изменения

---

## Чеклист безопасности для каждого шага:

### Перед началом:
- [ ] Создан git commit с текущим состоянием
- [ ] Работа в отдельной ветке
- [ ] Прочитан план для этого шага
- [ ] Понятны зависимости функции (что она использует, где используется)
- [ ] Проверены импорты и экспорты

### После изменений:
- [ ] Проверено через Telegram Mini App
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] Нет ошибок в логах терминала
- [ ] Протестированы все сценарии использования функции
- [ ] Импорты корректны (относительные пути)
- [ ] Экспорты корректны (export function/const)
- [ ] Создан git commit с описанием

### Через 1-2 дня:
- [ ] Все еще работает корректно
- [ ] Удален закомментированный код
- [ ] Создан финальный commit

---

## Оценка времени:

- **Этап 1 (admin_utils.js):** 1-2 часа
- **Этап 2 (admin_init.js):** 2-3 часа (функции инициализации)
- **Этап 3 (admin_settings.js):** 3-4 часа (несколько функций настроек)
- **Этап 4 (admin_stats.js):** 2-3 часа (большая функция рендеринга)
- **Этап 5 (admin_sold.js):** 3-4 часа (большая функция рендеринга)
- **Этап 6 (admin_orders.js):** 4-6 часов (очень большая функция, сложная логика)
- **Этап 7 (admin_purchases.js):** 5-7 часов (очень большая функция, работа с медиа)

**Общее время:** 20-29 часов работы

---

## Риски и митигация:

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Сломанная функциональность UI | Средняя | Высокое | Сохранение старого кода, постепенный переход, тестирование каждого шага |
| Проблемы с импортами/экспортами | Средняя | Среднее | Проверка путей импорта, использование относительных путей, тестирование |
| Конфликты в git | Низкая | Среднее | Работа в отдельной ветке, частые коммиты |
| Потеря производительности | Низкая | Низкое | Профилирование после рефакторинга |
| Проблемы с DOM манипуляциями | Средняя | Высокое | Особое внимание к функциям рендеринга, тестирование всех сценариев |
| Проблемы с медиа контентом (изображения, видео) | Средняя | Высокое | Особое внимание к loadPurchases, тестирование загрузки медиа |
| Проблемы с глобальными переменными | Средняя | Среднее | Проверка зависимостей от глобальных переменных (adminModal, shopSettings и т.д.) |

---

## Структура после рефакторинга:

```
webapp/js/
├── admin.js                    # ~100-200 строк (только инициализация, экспорты, импорты)
├── ADMIN_REFACTORING.md       # Этот файл
├── handlers/
│   ├── admin_init.js          # Инициализация и UI (initAdmin, createAdminModal, openAdmin, switchAdminTab)
│   ├── admin_settings.js      # Настройки магазина (handleQuantityEnabledToggle, handleReservationsToggle, checkAllProductsMadeToOrder, handleAllProductsMadeToOrderToggle, updateProductsUI)
│   ├── admin_orders.js         # Заказы (loadOrders)
│   ├── admin_sold.js           # Проданные товары (loadSoldProducts)
│   ├── admin_stats.js          # Статистика (loadStats)
│   └── admin_purchases.js      # Покупки (loadPurchases)
└── utils/
    └── admin_utils.js          # Утилиты (showNotification, getCurrentShopSettings, loadShopSettings)
```

---

## Важные замечания:

1. **Глобальные переменные:** `adminModal`, `reservationsToggle`, `quantityEnabledToggle`, `allProductsMadeToOrderToggle`, `shopSettings` должны быть доступны в модулях через экспорт/импорт или передаваться как параметры

2. **Импорты:** Все обработчики должны импортировать необходимые зависимости из соответствующих модулей (например, API функции из `api.js`)

3. **Экспорты:** Функции, используемые в других модулях, должны быть экспортированы (`export function` или `export async function`)

4. **Зависимости от API:** Все функции используют API функции из `api.js` - нужно убедиться, что импорты корректны

5. **Тестирование:** Каждый шаг должен быть протестирован через Telegram Mini App перед переходом к следующему

6. **Логирование:** Использовать `console.log()` для логирования в консоль браузера (не делаем тесты в браузере)

7. **DOM манипуляции:** Функции с большим количеством DOM манипуляций (loadOrders, loadSoldProducts, loadStats, loadPurchases) требуют особого внимания - нужно проверить, что все элементы создаются корректно

8. **Медиа контент:** Функция `loadPurchases()` работает с изображениями и видео через fetch и blob URLs - нужно убедиться, что эта логика работает корректно после рефакторинга

9. **Обработчики событий:** Многие функции устанавливают обработчики событий (onclick, onchange и т.д.) - нужно убедиться, что они работают после рефакторинга

10. **Зависимости от window:** Некоторые функции используют `window.getAppContext()`, `window.loadData()`, `window.updateShopNameInHeader()` - нужно убедиться, что эти зависимости доступны после рефакторинга

