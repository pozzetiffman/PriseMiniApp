# План рефакторинга app.js

## Статус рефакторинга

**Текущий размер файла:** ~610 строк (после выгрузки категорий, products, product-edit, reservations, orders, purchases, filters и modals)  
**Целевой размер:** ~800–900 строк  
**Выгружено:** ~2400+ строк (8 модулей: categories, products, product-edit, reservations, orders, purchases, filters, modals)

---

## Список функций для выгрузки из app.js

### ✅ 1. Модуль "categories.js" (категории) - **ВЫПОЛНЕНО**

- ✅ `renderCategories(categories)` — рендеринг категорий (~475 строк)
- ✅ `initCategoryFilterHandlers(filterDropdown)` — инициализация обработчиков фильтра (~90 строк)
- ✅ `updateCategoryFilter()` — обновление фильтра категорий (~55 строк)
- ✅ `updateCategoryFilterCount()` — обновление счетчика (~15 строк)
- ✅ Переменные состояния: `currentCategoryId`, `selectedCategoryIds`, `allCategories`, `categoriesHierarchy`, `selectedMainCategoryId`

**Статус:** Полностью выгружено, старый код удален

---

### ✅ 2. Модуль "products.js" (работа с товарами) - **ВЫПОЛНЕНО**

- ✅ `renderProducts(products)` — рендеринг товаров (~533 строки) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `showProductModal(prod, finalPrice, fullImages)` — показ модального окна товара (~420 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `showModalImage(index)` — показ изображения в модальном окне (~208 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `updateImageNavigation()` — обновление навигации по фото (~164 строки) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `isMobileDevice()` — детекция устройства (~9 строк) - **ВЫПОЛНЕНО** (старый код удалён)

**Статус:** ✅ **ВЫПОЛНЕНО** (5/5 функций)

---

### ✅ 3. Модуль "product-edit.js" (редактирование товаров) - **ВЫПОЛНЕНО**

- ✅ `showEditProductModal(prod)` — показ модального окна редактирования (~289 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `saveProductEdit(productId)` — сохранение изменений (~202 строки) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `deleteProduct(productId)` — удаление товара (~26 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `markAsSold(productId, product)` — пометить как проданный (~41 строка) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `showSellModal(productId, product)` — модальное окно продажи (~84 строки) - **ВЫПОЛНЕНО** (старый код удалён)

**Статус:** ✅ **ВЫПОЛНЕНО** (5/5 функций)

---

### ✅ 4. Модуль "reservations.js" (резервации) - **ВЫПОЛНЕНО**

- ✅ `showReservationModal(productId)` — показ модального окна резервации (~122 строки) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `createReservation(productId, hours, quantity)` — создание резервации (~29 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `cancelReservation(reservationId, productId)` — отмена резервации (~30 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `window.cancelReservationFromCart()` — отмена из корзины (~5 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `window.clearReservationsHistory()` — очистка истории (~22 строки) - **ВЫПОЛНЕНО** (старый код удалён)

**Статус:** ✅ **ВЫПОЛНЕНО** (5/5 функций) - старый код удалён

---

### ✅ 5. Модуль "orders.js" (заказы) - **ВЫПОЛНЕНО**

- ✅ `showOrderModal(productId)` — показ модального окна заказа (~34 строки) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `resetOrderForm()` — сброс формы заказа (~12 строк) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `updateOrderProductSummary(product)` — обновление информации о товаре (~26 строк) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `showOrderStep(step)` — показ шага формы (~15 строк) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `setupOrderFormHandlers(productId)` — настройка обработчиков (~58 строк) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `submitOrder(productId)` — отправка заказа (~47 строк) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `createOrder(productId, quantity)` — создание заказа (legacy, ~4 строки) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `cancelOrder(orderId)` — отмена заказа (~28 строк) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `window.cancelOrderFromCart()` — отмена из корзины (~6 строк) - **ВЫПОЛНЕНО** (старый код закомментирован)
- ✅ `window.clearOrdersHistory()` — очистка истории (~22 строки) - **ВЫПОЛНЕНО** (старый код закомментирован)

**Статус:** ✅ **ВЫПОЛНЕНО** (10/10 функций) - старый код удален

---

### ✅ 6. Модуль "purchases.js" (продажи/покупки)

- ✅ `showPurchaseModal(prod)` — показ модального окна продажи (~98 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `submitPurchaseForm(productId)` — отправка формы продажи (~91 строка) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `cancelPurchase(purchaseId)` — отмена продажи (~28 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `window.cancelPurchaseFromCart()` — отмена из корзины (~6 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `window.clearPurchasesHistory()` — очистка истории (~22 строки) - **ВЫПОЛНЕНО** (старый код удалён)

**Статус:** ✅ **ВЫПОЛНЕНО** (5/5 функций) - старый код удалён

---

### ✅ 7. Модуль "filters.js" (фильтры товаров)

- ✅ `initFilters()` — инициализация фильтров (~3 строки) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `updateProductFilterOptions()` — обновление опций фильтра (~93 строки) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `applyFilters()` — применение фильтров (~130 строк) - **ВЫПОЛНЕНО** (старый код удалён)

**Статус:** ✅ **ВЫПОЛНЕНО** (3/3 функций) - старый код удалён

---

### ✅ 8. Модуль "modals.js" (настройка модальных окон) - **ВЫПОЛНЕНО**

- ✅ `setupModals()` — настройка модальных окон (~138 строк) - **ВЫПОЛНЕНО** (старый код удалён)

**Статус:** ✅ **ВЫПОЛНЕНО** (1/1 функций) - старый код удалён

---

### ✅ 9. Модуль "data.js" (загрузка данных) - **ВЫПОЛНЕНО**

- ✅ `window.loadData()` — загрузка данных (категории и товары) (~93 строки) - **ВЫПОЛНЕНО** (старый код удалён)
- ✅ `window.updateShopNameInHeader()` — обновление заголовка (~26 строк) - **ВЫПОЛНЕНО** (старый код удалён)

**Статус:** ✅ **ВЫПОЛНЕНО** (2/2 функций) - старый код удалён

---

## Итого:

- **Всего функций:** ~38
- **Приблизительный объем кода для выгрузки:** ~2500+ строк
- **Текущий размер файла:** ~610 строк (было 2939)
- **Выгружено:** ~2500+ строк (9 модулей: categories, products, product-edit, reservations, orders, purchases, filters, modals, data.js)
- **После выгрузки останется:** ~600–700 строк (инициализация, глобальные переменные, координация модулей)

---

## Рекомендуемый порядок выгрузки:

1. ✅ **categories.js** - ВЫПОЛНЕНО
2. ✅ **products.js** - ВЫПОЛНЕНО
3. ✅ **product-edit.js** - ВЫПОЛНЕНО
4. ✅ **reservations.js** - ВЫПОЛНЕНО
5. ✅ **orders.js** - ВЫПОЛНЕНО
6. ✅ **purchases.js** - ВЫПОЛНЕНО
7. ✅ **filters.js** - ВЫПОЛНЕНО
8. ✅ **modals.js** - ВЫПОЛНЕНО
9. ✅ **data.js** - ВЫПОЛНЕНО

---

## Легенда статусов:

- ✅ **ВЫПОЛНЕНО** - Функция выгружена, протестирована, старый код удален
- 🔄 **В ПРОЦЕССЕ** - Частично выполнено
- ⏳ **НЕ НАЧАТО** - Ожидает выполнения

---

## Примечания:

- Рефакторинг проводится мягко и постепенно, по одной функции
- Старый код сначала закомментирован для проверки работоспособности
- После подтверждения работоспособности старый код удаляется
- Все зависимости передаются через инициализацию модулей

