# План рефакторинга bot/bot.py

## Статус рефакторинга

**Текущий размер файла:** ~2686 строк  
**Количество функций/обработчиков:** ~72  
**Целевой размер:** ~200–300 строк (только регистрация обработчиков и main)  
**Планируется выгрузить:** ~2400+ строк (утилиты, состояния, обработчики)

---

## Список функций для выгрузки из bot/bot.py

### 🔄 1. Модуль "bot/utils.py" (утилиты) - **В ПРОЦЕССЕ**

- 🔄 `get_bot_username()` — получение username бота (~7 строк) - **В ПРОЦЕССЕ**
  - Используется в: `get_bot_deeplink()`, `cmd_start()`, `cmd_post()`, `send_store_to_channel()`
  - Приоритет: 🟡 СРЕДНИЙ (используется в нескольких местах, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

- 🔄 `get_bot_deeplink(user_id)` — получение deep link на бота (~4 строки) - **В ПРОЦЕССЕ**
  - Используется в: `cmd_post()`, `send_store_to_channel()`
  - Приоритет: 🟡 СРЕДНИЙ (используется в нескольких местах, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

- 🔄 `get_shop_name(user_id)` — получение названия магазина (~12 строк) - **В ПРОЦЕССЕ**
  - Используется в: `cmd_start()`, `cmd_post()`, `send_store_to_channel()`
  - Приоритет: 🟡 СРЕДНИЙ (используется в нескольких местах, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

- 🔄 `get_shop_settings(user_id)` — получение настроек магазина (~11 строк) - **В ПРОЦЕССЕ**
  - Используется в: `send_shop_message()`, `cmd_start()`, `cmd_post()`, `send_store_to_channel()`, `manage_shop_name()`, `manage_welcome_image()`, `manage_welcome_description()`
  - Приоритет: 🔴 ВЫСОКИЙ (используется во многих местах, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

- 🔄 `send_shop_message(bot_or_message, chat_id_or_message, msg, reply_markup, shop_owner_id)` — отправка сообщения о магазине (~42 строки) - **В ПРОЦЕССЕ**
  - Используется в: `cmd_post()`
  - Приоритет: 🟡 СРЕДНИЙ (используется в одном месте, но сложная логика)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

- 🔄 `is_command(text)` — проверка, является ли текст командой (~5 строк) - **В ПРОЦЕССЕ**
  - Используется в: `clear_state_if_needed()`, `process_bot_token()`, `process_web_app_name()`, `process_category_name()`, `process_name()`, `process_price_from()`, `process_price_to()`, `process_price_fixed()`, `process_quantity_from()`, `process_price()`, `process_discount()`, `process_description()`, `process_quantity()`, `process_photos_done()`, `process_shop_name()`, `process_welcome_image_text()`, `process_welcome_description()`
  - Приоритет: 🔴 ВЫСОКИЙ (используется во многих местах, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

- 🔄 `is_menu_button(text)` — проверка, является ли текст кнопкой меню (~19 строк) - **В ПРОЦЕССЕ**
  - Используется в: `clear_state_if_needed()`, `process_bot_token()`, `process_web_app_name()`, `process_category_name()`, `process_name()`, `process_price_from()`, `process_price_to()`, `process_price_fixed()`, `process_quantity_from()`, `process_price()`, `process_discount()`, `process_description()`, `process_quantity()`, `process_photos_done()`, `process_shop_name()`, `process_welcome_image_text()`, `process_welcome_description()`
  - Приоритет: 🔴 ВЫСОКИЙ (используется во многих местах, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

- 🔄 `clear_state_if_needed(message, state, current_state)` — очистка состояния FSM (~57 строк) - **В ПРОЦЕССЕ**
  - Используется в: `cmd_start()`, `cmd_getlink()`, `cmd_connect()`, `cmd_post()`, `cmd_mylink()`, `cmd_manage()`, `start_add_category()`, `list_categories()`, `manage_shop_name()`, `manage_welcome_image()`, `manage_welcome_description()`, `manage_channels()`, `delete_product_start()`, `get_my_links_button()`, `connect_bot_button()`, `share_store()`
  - Приоритет: 🔴 ВЫСОКИЙ (используется во многих местах, средний риск - делать осторожно!)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/utils.py, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ** (get_bot_username перенесена в bot/utils.py, старый код закомментирован, ожидает проверки)

**Приоритет:** 🔴 ВЫСОКИЙ (базовые утилиты, используются везде)

---

### 🔄 2. Модуль "bot/states.py" (состояния FSM) - **ЗАВЕРШЕНО** ✅

- 🔄 `AddCategory` — состояния для добавления категории (~3 строки) - **В ПРОЦЕССЕ**
  - Используется в: `start_add_category()`, `process_category_name()`, `create_main_category()`, `choose_parent_category()`, `create_subcategory()`
  - Приоритет: 🟢 НИЗКИЙ (изолированный класс, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Класс перенесен в bot/states.py, старый код закомментирован, ожидает проверки

- 🔄 `AddProduct` — состояния для добавления товара (~17 строк) - **В ПРОЦЕССЕ**
  - Используется в: `start_add_product()`, `process_name()`, `process_product_type()`, `process_price_type()`, `process_price_from()`, `process_price_to()`, `process_price_fixed()`, `process_quantity_from()`, `process_quantity_unit()`, `process_quantity_show_enabled()`, `process_price()`, `process_category()`, `process_hot_offer()`, `process_discount()`, `process_description()`, `process_quantity()`, `process_photos()`, `process_photos_done()`
  - Приоритет: 🟡 СРЕДНИЙ (используется во многих местах, но изолированный класс)
  - Дата начала: 2024-12-19
  - Статус: Класс перенесен в bot/states.py, старый код закомментирован, ожидает проверки

- 🔄 `AddChannel` — состояния для добавления канала (~2 строки) - **В ПРОЦЕССЕ**
  - Используется в: (пока не используется, но может использоваться в будущем), `clear_state_if_needed()`
  - Приоритет: 🟢 НИЗКИЙ (не используется, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Класс перенесен в bot/states.py, старый код закомментирован, ожидает проверки

- 🔄 `SetShopName` — состояния для установки названия магазина (~2 строки) - **В ПРОЦЕССЕ**
  - Используется в: `manage_shop_name()`, `process_shop_name()`, `clear_state_if_needed()`
  - Приоритет: 🟢 НИЗКИЙ (изолированный класс, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Класс перенесен в bot/states.py, старый код закомментирован, ожидает проверки

- 🔄 `SetWelcomeImage` — состояния для установки логотипа (~2 строки) - **В ПРОЦЕССЕ**
  - Используется в: `manage_welcome_image()`, `process_welcome_image()`, `process_welcome_image_text()`, `clear_state_if_needed()`
  - Приоритет: 🟢 НИЗКИЙ (изолированный класс, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Класс перенесен в bot/states.py, старый код закомментирован, ожидает проверки

- 🔄 `SetWelcomeDescription` — состояния для установки описания (~2 строки) - **В ПРОЦЕССЕ**
  - Используется в: `manage_welcome_description()`, `process_welcome_description()`, `clear_state_if_needed()`
  - Приоритет: 🟢 НИЗКИЙ (изолированный класс, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Класс перенесен в bot/states.py, старый код закомментирован, ожидает проверки

- 🔄 `ConnectBot` — состояния для подключения бота (~3 строки) - **В ПРОЦЕССЕ**
  - Используется в: `cmd_connect()`, `process_bot_token()`, `process_web_app_name()`, `clear_state_if_needed()`
  - Приоритет: 🟢 НИЗКИЙ (изолированный класс, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Класс перенесен в bot/states.py, старый код закомментирован, ожидает проверки

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟢 НИЗКИЙ (изолированные классы, низкий риск - можно делать первым)

---

### 🔄 3. Модуль "bot/handlers/commands.py" (команды бота) - **В ПРОЦЕССЕ**

- 🔄 `cmd_cancel(message, state)` — отмена текущей операции (~22 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(Command("cancel"))`
  - Приоритет: 🟡 СРЕДНИЙ (изолированная команда, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки

- 🔄 `cmd_start(message, command, state)` — команда /start (~63 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(Command("start"))`
  - Приоритет: 🔴 ВЫСОКИЙ (критичная команда, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки

- 🔄 `cmd_getlink(message, command, state)` — команда /getlink (~147 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(Command("getlink"))`
  - Приоритет: 🟡 СРЕДНИЙ (изолированная команда, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки

- 🔄 `cmd_connect(message, state)` — команда /connect (~33 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(Command("connect"))`
  - Приоритет: 🟡 СРЕДНИЙ (изолированная команда, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки

- 🔄 `cmd_post(message, state)` — команда /post (~120 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(Command("post"))` и `@dp.message(F.text == "/post")`
  - Приоритет: 🟡 СРЕДНИЙ (изолированная команда, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки

- 🔄 `cmd_mylink(message, state)` — команда /mylink (~5 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(Command("mylink"))`
  - Приоритет: 🟡 СРЕДНИЙ (обертка над `_cmd_mylink_impl`, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки
  - Примечание: Использует локальную реализацию `_cmd_mylink_impl` из того же модуля

- 🔄 `_cmd_mylink_impl(message)` — реализация команды /mylink (~85 строк) - **В ПРОЦЕССЕ**
  - Используется в: `cmd_mylink()`, `get_my_links_button()`, `delete_bot_callback()`
  - Приоритет: 🟡 СРЕДНИЙ (изолированная функция, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки
  - Примечание: Функция импортирована в bot.py для использования в `get_my_links_button()` и `delete_bot_callback()`

- 🔄 `cmd_manage(message, state)` — команда /manage (~5 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(Command("manage"))`
  - Приоритет: 🟡 СРЕДНИЙ (обертка над `_cmd_manage_impl`, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки
  - Примечание: Использует локальную реализацию `_cmd_manage_impl` из того же модуля

- 🔄 `_cmd_manage_impl(message)` — реализация команды /manage (~19 строк) - **В ПРОЦЕССЕ**
  - Используется в: `cmd_manage()`, `create_main_category()`, `create_subcategory()`, `delete_category_confirm()`, `process_shop_name()`, `process_welcome_image()`, `process_welcome_image_text()`, `process_welcome_description()`, `delete_product_confirm()`, `process_photos_done()`
  - Приоритет: 🟡 СРЕДНИЙ (используется во многих местах, но простая функция)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/commands.py, старый код закомментирован, ожидает проверки
  - Примечание: Функция импортирована в bot.py для использования в других функциях, которые еще не перенесены

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (изолированные команды, средний риск)

---

### 🔄 4. Модуль "bot/handlers/categories.py" (управление категориями) - **В ПРОЦЕССЕ**

- 🔄 `start_add_category(message, state)` — начало добавления категории (~5 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "📁 Добавить категорию")`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

- 🔄 `process_category_name(message, state)` — обработка названия категории (~48 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddCategory.name)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

- 🔄 `create_main_category(callback, state)` — создание основной категории (~20 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddCategory.parent_choice), F.data == "cat_main")`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

- 🔄 `choose_parent_category(callback, state)` — выбор родительской категории (~28 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddCategory.parent_choice), F.data == "cat_sub")`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

- 🔄 `create_subcategory(callback, state)` — создание подкатегории (~22 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddCategory.parent_choice), F.data.startswith("parent_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

- 🔄 `list_categories(message, state)` — список категорий (~69 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "📋 Список категорий")`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

- 🔄 `delete_category_confirm(callback)` — подтверждение удаления категории (~21 строка) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(F.data.startswith("del_category_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

- 🔄 `show_category_selection(callback_or_message, state)` — показ выбора категории (~54 строки) - **В ПРОЦЕССЕ**
  - Используется в: `process_quantity_show_enabled()`, `process_price()`
  - Приоритет: 🟡 СРЕДНИЙ (используется в товарах, но логически относится к категориям)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/categories.py, старый код закомментирован, ожидает проверки

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟢 НИЗКИЙ (изолированные обработчики, низкий риск - можно делать рано)

---

### 🔄 5. Модуль "bot/handlers/products.py" (управление товарами) - **В ПРОЦЕССЕ**

- 🔄 `start_add_product(message, state)` — начало добавления товара (~6 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "➕ Добавить товар")`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_name(message, state)` — обработка названия товара (~20 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.name)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_product_type(callback, state)` — обработка типа товара (~40 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddProduct.product_type), F.data.startswith("product_type_"))`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_price_type(callback, state)` — обработка типа цены (~13 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddProduct.price_type), F.data.startswith("price_type_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_price_from(message, state)` — обработка цены "от" (~11 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.price_from)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_price_to(message, state)` — обработка цены "до" (~36 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.price_to)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_price_fixed(message, state)` — обработка фиксированной цены (~25 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.price_fixed)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_quantity_from(message, state)` — обработка количества "от" (~32 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.quantity_from)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_quantity_unit(callback, state)` — обработка единицы измерения (~33 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddProduct.quantity_unit), F.data.startswith("unit_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_quantity_show_enabled(callback, state)` — обработка показа количества (~27 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddProduct.quantity_show_enabled), F.data.startswith("quantity_show_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_price(message, state)` — обработка цены (~62 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.price)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_category(callback, state)` — обработка категории (~15 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddProduct.category))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_hot_offer(callback, state)` — обработка горящего предложения (~16 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(StateFilter(AddProduct.is_hot_offer), F.data.startswith("hot_offer_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_discount(message, state)` — обработка скидки (~9 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.discount)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_description(message, state)` — обработка описания (~50 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.description)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_quantity(message, state)` — обработка количества (~48 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.quantity)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_photos(message, state)` — обработка фото (~76 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.photos, F.photo)`
  - Приоритет: 🔴 ВЫСОКИЙ (сложная логика с файлами, высокий риск - делать осторожно!)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `process_photos_done(message, state)` — завершение обработки фото (~135 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(AddProduct.photos)`
  - Приоритет: 🔴 ВЫСОКИЙ (сложная логика с файлами и API, высокий риск - делать осторожно!)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `delete_product_start(message, state)` — начало удаления товара (~33 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "🗑️ Удалить товар")`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

- 🔄 `delete_product_confirm(callback)` — подтверждение удаления товара (~19 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(F.data.startswith("del_product_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ** (start_add_product перенесена в bot/handlers/products.py, старый код закомментирован, ожидает проверки)

**Приоритет:** 🟡 СРЕДНИЙ (много обработчиков, но изолированных, средний риск - делать по одному)

---

### 🔄 6. Модуль "bot/handlers/shop_settings.py" (настройки магазина) - **В ПРОЦЕССЕ**

- 🔄 `manage_shop_name(message, state)` — управление названием магазина (~27 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "🏷️ Название магазина")`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/shop_settings.py, старый код закомментирован, ожидает проверки

- 🔄 `process_shop_name(message, state)` — обработка названия магазина (~48 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(SetShopName.name)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/shop_settings.py, старый код закомментирован, ожидает проверки

- 🔄 `manage_welcome_image(message, state)` — управление логотипом (~28 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "🖼️ Логотип магазина")`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/shop_settings.py, старый код закомментирован, ожидает проверки

- 🔄 `process_welcome_image(message, state)` — обработка логотипа (фото) (~45 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(SetWelcomeImage.image, F.photo)`
  - Приоритет: 🟡 СРЕДНИЙ (работа с файлами, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/shop_settings.py, старый код закомментирован, ожидает проверки

- 🔄 `process_welcome_image_text(message, state)` — обработка логотипа (текст) (~28 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(SetWelcomeImage.image)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/shop_settings.py, старый код закомментирован, ожидает проверки

- 🔄 `manage_welcome_description(message, state)` — управление описанием (~29 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "📝 Описание магазина")`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/shop_settings.py, старый код закомментирован, ожидает проверки

- 🔄 `process_welcome_description(message, state)` — обработка описания (~51 строка) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(SetWelcomeDescription.description)`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/shop_settings.py, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ** (все функции модуля shop_settings перенесены в bot/handlers/shop_settings.py: manage_shop_name, process_shop_name, manage_welcome_image, process_welcome_image, process_welcome_image_text, manage_welcome_description и process_welcome_description; старый код закомментирован, ожидает проверки)

**Приоритет:** 🟢 НИЗКИЙ (изолированные обработчики, низкий риск - можно делать рано)

---

### 🔄 7. Модуль "bot/handlers/channels.py" (управление каналами) - **В ПРОЦЕССЕ**

- 🔄 `share_store(message, state)` — поделиться витриной (~46 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "📤 Поделиться витриной")`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/channels.py, старый код закомментирован, ожидает проверки

- 🔄 `manage_channels(message, state)` — управление каналами (~48 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text == "📢 Управление каналами")`, также вызывается из `delete_channel` callback
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/channels.py, старый код закомментирован, ожидает проверки
  - Примечание: Функция принимает опциональный параметр `state` для совместимости с вызовом из callback

- 🔄 `add_channel_by_username(message)` — добавление канала по username (~44 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.text.startswith("@"))`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/channels.py, старый код закомментирован, ожидает проверки

- 🔄 `add_channel_by_forward(message)` — добавление канала через пересылку (~40 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.message(F.forward_from_chat)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/channels.py, старый код закомментирован, ожидает проверки

- 🔄 `send_store_to_channel(callback)` — отправка витрины в канал (~102 строки) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(F.data.startswith("share_"))`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/channels.py, старый код закомментирован, ожидает проверки

- 🔄 `delete_channel(callback)` — удаление канала (~13 строк) - **В ПРОЦЕССЕ**
  - Используется в: регистрация через `@dp.callback_query(F.data.startswith("del_channel_"))`
  - Приоритет: 🟢 НИЗКИЙ (изолированный обработчик, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция перенесена в bot/handlers/channels.py, старый код закомментирован, ожидает проверки
  - Примечание: Функция использует `manage_channels` для обновления сообщения после удаления

**Статус:** 🔄 **В ПРОЦЕССЕ** (все функции модуля channels перенесены в bot/handlers/channels.py: share_store, manage_channels, add_channel_by_username, add_channel_by_forward, send_store_to_channel и delete_channel; старый код закомментирован, ожидает проверки)

**Приоритет:** 🟡 СРЕДНИЙ (изолированные обработчики, средний риск)

---

### 🔄 8. Модуль "bot/handlers/bots.py" (управление ботами) - **НЕ НАЧАТО**

- ⏳ `process_bot_token(message, state)` — обработка токена бота (~40 строк)
  - Используется в: регистрация через `@dp.message(ConnectBot.token)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)

- ⏳ `process_web_app_name(message, state)` — обработка названия Web App (~103 строки)
  - Используется в: регистрация через `@dp.message(ConnectBot.web_app_name)`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, средний риск)

- ⏳ `get_my_links_button(message, state)` — кнопка "Мои ссылки" (~5 строк)
  - Используется в: регистрация через `@dp.message(F.text == "🔗 Мои ссылки")`
  - Приоритет: 🟢 НИЗКИЙ (обертка над `_cmd_mylink_impl`, низкий риск)

- ⏳ `connect_bot_button(message, state)` — кнопка "Подключить бота" (~5 строк)
  - Используется в: регистрация через `@dp.message(F.text == "🤖 Подключить бота")`
  - Приоритет: 🟢 НИЗКИЙ (обертка над `cmd_connect`, низкий риск)

- ⏳ `delete_bot_callback(callback)` — удаление бота (~43 строки)
  - Используется в: регистрация через `@dp.callback_query(F.data.startswith("delete_bot_"))`
  - Приоритет: 🟡 СРЕДНИЙ (изолированный обработчик, низкий риск)

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (изолированные обработчики, средний риск)

---

### 🔄 9. Модуль "bot/handlers/notifications.py" (уведомления) - **НЕ НАЧАТО**

- ⏳ `send_reservation_notification(product_owner_id, product_id, reserved_by_user_id, reserved_until, product_name)` — отправка уведомления о резервации (~50 строк)
  - Используется в: (вызывается из backend, не из bot.py напрямую)
  - Приоритет: 🟢 НИЗКИЙ (изолированная функция, низкий риск)

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟢 НИЗКИЙ (изолированная функция, низкий риск)

---

## Итого:

- **Всего функций/обработчиков:** ~72
- **Приблизительный объем кода для выгрузки:** ~2400+ строк
- **Текущий размер файла:** ~2686 строк
- **После выгрузки останется:** ~200–300 строк (регистрация обработчиков, main, импорты)

---

## Рекомендуемый порядок выгрузки:

1. ⏳ **bot/states.py** - НЕ НАЧАТО (STEP 1) - Все классы состояний FSM
   - Приоритет: 🟢 НИЗКИЙ (изолированные классы, низкий риск - можно делать первым)

2. ⏳ **bot/utils.py** - НЕ НАЧАТО (STEP 2) - Базовые утилиты
   - Приоритет: 🔴 ВЫСОКИЙ (используются везде, но низкий риск - делать осторожно!)

3. ⏳ **bot/handlers/notifications.py** - НЕ НАЧАТО (STEP 3) - Уведомления
   - Приоритет: 🟢 НИЗКИЙ (изолированная функция, низкий риск)

4. ⏳ **bot/handlers/categories.py** - НЕ НАЧАТО (STEP 4) - Управление категориями
   - Приоритет: 🟢 НИЗКИЙ (изолированные обработчики, низкий риск - можно делать рано)

5. ⏳ **bot/handlers/shop_settings.py** - НЕ НАЧАТО (STEP 5) - Настройки магазина
   - Приоритет: 🟢 НИЗКИЙ (изолированные обработчики, низкий риск - можно делать рано)

6. ⏳ **bot/handlers/channels.py** - НЕ НАЧАТО (STEP 6) - Управление каналами
   - Приоритет: 🟡 СРЕДНИЙ (изолированные обработчики, средний риск)

7. ⏳ **bot/handlers/bots.py** - НЕ НАЧАТО (STEP 7) - Управление ботами
   - Приоритет: 🟡 СРЕДНИЙ (изолированные обработчики, средний риск)

8. ⏳ **bot/handlers/commands.py** - НЕ НАЧАТО (STEP 8) - Команды бота
   - Приоритет: 🟡 СРЕДНИЙ (изолированные команды, средний риск)

9. ⏳ **bot/handlers/products.py** - НЕ НАЧАТО (STEP 9) - Управление товарами
   - Приоритет: 🟡 СРЕДНИЙ (много обработчиков, но изолированных, средний риск - делать по одному, особенно осторожно с process_photos и process_photos_done!)

---

## Легенда статусов:

- ✅ **ВЫПОЛНЕНО** - Функция выгружена, протестирована, старый код удален
- 🔄 **В ПРОЦЕССЕ** - Частично выполнено, старый код закомментирован
- ⏳ **НЕ НАЧАТО** - Ожидает выполнения

---

## Примечания:

- Рефакторинг проводится мягко и постепенно, по одной функции
- Старый код сначала закомментирован для проверки работоспособности
- После подтверждения работоспособности (через 1-2 дня) старый код удаляется
- Каждый шаг тестируется отдельно перед переходом к следующему
- Критичные функции (cmd_start, process_photos, process_photos_done) рефакторятся осторожно
- Функции с файлами (process_photos, process_welcome_image) требуют особого внимания

---

## Шаблон для каждого шага:

### Перед началом:
```python
# TODO: REFACTORING STEP X.Y - [Название шага]
# Дата начала: YYYY-MM-DD
# Статус: В процессе / Завершен / Откат
```

### В процессе рефакторинга:
```python
# ========== REFACTORING STEP X.Y: [Название] ==========
# НОВЫЙ КОД (используется сейчас)
from bot.handlers.new_module import new_function
result = await new_function(params)

# СТАРЫЙ КОД (закомментирован, будет удален после проверки)
"""
async def old_function(params):
    # старая реализация
"""
# ========== END REFACTORING STEP X.Y ==========
```

### После проверки (через 1-2 дня):
- Если все работает: удалить закомментированный код
- Если есть проблемы: откатить изменения

---

## Чеклист безопасности для каждого шага:

### Перед началом:
- [ ] Создан git commit с текущим состоянием
- [ ] Работа в отдельной ветке
- [ ] Прочитан план для этого шага
- [ ] Понятны зависимости функции (что она использует, где используется)

### После изменений:
- [ ] Проверено в Telegram боте
- [ ] Нет ошибок в логах
- [ ] Протестированы все сценарии использования функции
- [ ] Импорты корректны (относительные пути)
- [ ] Создан git commit с описанием

### Через 1-2 дня:
- [ ] Все еще работает корректно
- [ ] Удален закомментированный код
- [ ] Создан финальный commit

---

## Оценка времени:

- **Этап 1 (bot/states.py):** 1-2 часа
- **Этап 2 (bot/utils.py):** 3-4 часа
- **Этап 3 (bot/handlers/notifications.py):** 1 час
- **Этап 4 (bot/handlers/categories.py):** 3-4 часа
- **Этап 5 (bot/handlers/shop_settings.py):** 2-3 часа
- **Этап 6 (bot/handlers/channels.py):** 2-3 часа
- **Этап 7 (bot/handlers/bots.py):** 2-3 часа
- **Этап 8 (bot/handlers/commands.py):** 4-6 часов
- **Этап 9 (bot/handlers/products.py):** 6-10 часов (много обработчиков, особенно осторожно с файлами)

**Общее время:** 24-36 часов работы

---

## Риски и митигация:

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Сломанная функциональность бота | Средняя | Высокое | Сохранение старого кода, постепенный переход, тестирование каждого шага |
| Проблемы с импортами | Низкая | Среднее | Проверка путей импорта, использование относительных путей, тестирование |
| Конфликты в git | Низкая | Среднее | Работа в отдельной ветке, частые коммиты |
| Потеря производительности | Низкая | Низкое | Профилирование после рефакторинга |
| Проблемы с FSM состояниями | Средняя | Высокое | Особое внимание к состояниям, тестирование переходов между состояниями |
| Проблемы с файлами (фото) | Средняя | Высокое | Особое внимание к process_photos и process_photos_done, тестирование загрузки файлов |

---

## Структура после рефакторинга:

```
bot/
├── __init__.py
├── bot.py                    # ~200-300 строк (только регистрация обработчиков и main)
├── config.py                 # (существует)
├── keyboards.py              # (существует)
├── bot_manager.py            # (существует)
├── states.py                 # Все классы состояний FSM
├── utils.py                  # Утилиты (get_bot_username, get_shop_settings, etc.)
└── handlers/
    ├── __init__.py
    ├── commands.py           # Команды бота (/start, /manage, /post, etc.)
    ├── categories.py         # Управление категориями
    ├── products.py           # Управление товарами
    ├── shop_settings.py      # Настройки магазина
    ├── channels.py           # Управление каналами
    ├── bots.py               # Управление ботами
    └── notifications.py      # Уведомления
```

---

## Важные замечания:

1. **Глобальные переменные:** `bot`, `dp`, `TOKEN`, `WEBAPP_URL`, `API_URL`, `_bot_username` должны остаться в `bot.py` или быть вынесены в `config.py`

2. **Импорты:** Все обработчики должны импортировать `bot`, `dp` из основного модуля или через `config.py`

3. **Регистрация обработчиков:** После выгрузки обработчиков в отдельные модули, их нужно зарегистрировать в `bot.py` через импорты

4. **FSM состояния:** Состояния должны быть импортированы в обработчики из `states.py`

5. **Тестирование:** Каждый шаг должен быть протестирован в реальном Telegram боте перед переходом к следующему

