# План рефакторинга cart.js

## Статус рефакторинга

**Текущий размер файла:** ~1973 строки  
**Целевой размер:** ~400–500 строк  
**Планируется выгрузить:** ~1500+ строк (утилиты и модули)

---

## Список функций для выгрузки из cart.js

### 🔄 1. Модуль "utils/priceUtils.js" (утилиты для цен) - **В ПРОЦЕССЕ**

- ✅ `getProductPriceDisplay(prod)` — форматирование цены товара (~52 строки, дублируется 4 раза)
  - Используется в: `loadCart()`, `loadOrders()`, `loadReservationsHistory()`, `loadOrdersHistory()`
  - ✅ Функция вынесена в модуль `utils/priceUtils.js`
  - ✅ Все 4 использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)

**Статус:** 🔄 **В ПРОЦЕССЕ** (STEP 1.1 завершен, старый код закомментирован)

**Приоритет:** 🔴 ВЫСОКИЙ (дублируется 4 раза, низкий риск)

---

### ✅ 2. Модуль "utils/imageUtils.js" (утилиты для изображений) - **ЗАВЕРШЕНО**

- ✅ `getProductImageUrl(product, API_BASE)` — получение URL изображения товара (~15 строк, дублируется ~8 раз)
  - Используется в: `loadCart()`, `loadOrders()`, `loadPurchases()`, `loadReservationsHistory()`, `loadOrdersHistory()`, `loadPurchasesHistory()`
  - ✅ Функция вынесена в модуль `utils/imageUtils.js`
  - ✅ Все 6 использований заменены на импортированную версию
  - ✅ Протестировано: работает корректно (товары с изображениями отображаются, без изображений показывается заглушка 📦)
  - 🔄 Старый код закомментирован, ожидает удаления (1-2 дня)
- ✅ `createImageContainer(imageUrl, productName)` — создание контейнера изображения (~50 строк, дублируется ~8 раз)
  - Используется в: `loadCart()`, `loadOrders()`, `loadPurchases()`, `loadReservationsHistory()`, `loadOrdersHistory()`, `loadPurchasesHistory()`
  - ✅ Функция вынесена в модуль `utils/imageUtils.js`
  - ✅ Все 6 использований заменены на импортированную версию
  - ✅ Протестировано: работает корректно (изображения загружаются через fetch, ошибки логируются)
  - 🔄 Старый код закомментирован, ожидает удаления (1-2 дня)

**Статус:** ✅ **ЗАВЕРШЕНО** (STEP 2.1 и STEP 2.2 завершены и протестированы, старый код закомментирован, ожидает удаления)

**Приоритет:** 🟡 СРЕДНИЙ (дублируется много раз, низкий риск)

---

### 🔄 3. Модуль "utils/dateUtils.js" (утилиты для дат) - **В ПРОЦЕССЕ**

- ✅ `formatDateToMoscow(dateString)` — форматирование даты в московское время (~20 строк, дублируется ~6 раз)
  - Используется в: `loadCart()`, `loadOrders()`, `loadPurchases()`, `loadReservationsHistory()`, `loadOrdersHistory()`, `loadPurchasesHistory()`
  - ✅ Функция вынесена в модуль `utils/dateUtils.js`
  - ✅ Все 6 использований заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)
- ✅ `calculateReservationTimeLeft(reservedUntilStr)` — расчет времени до истечения резервации (~30 строк)
  - Используется в: `loadCart()`
  - ✅ Функция вынесена в модуль `utils/dateUtils.js`
  - ✅ Использование заменено на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)

**Статус:** 🔄 **В ПРОЦЕССЕ** (STEP 3.1 и STEP 3.2 завершены, старый код закомментирован)

**Приоритет:** 🟡 СРЕДНИЙ (дублируется несколько раз, низкий риск)

---

### 🔄 4. Модуль "cart/cartHistory.js" (история корзины) - **В ПРОЦЕССЕ**

- ✅ `loadReservationsHistory()` — загрузка истории резерваций (~252 строки)
  - Используется в: `cart.js`, `reservations.js`
  - ✅ Функция вынесена в модуль `cart/cartHistory.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)
- ✅ `loadOrdersHistory()` — загрузка истории заказов (~249 строк)
  - Используется в: `cart.js`, `orders.js`
  - ✅ Функция вынесена в модуль `cart/cartHistory.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)
- ✅ `loadPurchasesHistory()` — загрузка истории продаж (~182 строки)
  - Используется в: `cart.js`, `purchases.js`
  - ✅ Функция вынесена в модуль `cart/cartHistory.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)

**Статус:** 🔄 **В ПРОЦЕССЕ** (STEP 4.1, 4.2, 4.3 завершены, старый код закомментирован)

**Приоритет:** 🟡 СРЕДНИЙ (изолированные функции, средний риск)

---

### 🔄 5. Модуль "cart/cartActive.js" (активные элементы корзины) - **В ПРОЦЕССЕ**

- ✅ `loadCart()` — загрузка активных резерваций (~289 строк)
  - Используется в: `cart.js`, `reservations.js` (через callback)
  - ✅ Функция вынесена в модуль `cart/cartActive.js`
  - ✅ Все использования заменены на импортированную версию
  - ✅ Старый код закомментирован
  - ✅ **ПРОТЕСТИРОВАНО**: Корзина успешно загружается в Telegram (мобильная и десктопная версии)
- ✅ `loadOrders()` — загрузка активных заказов (~232 строки)
  - Используется в: `cart.js`, `orders.js` (через callback), `app.js`
  - ✅ Функция вынесена в модуль `cart/cartActive.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)
- ✅ `loadPurchases()` — загрузка активных продаж (~173 строки)
  - Используется в: `cart.js`, `purchases.js` (через callback), `app.js`
  - ✅ Функция вынесена в модуль `cart/cartActive.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)

**Статус:** 🔄 **В ПРОЦЕССЕ** (STEP 5.1 завершен и протестирован, STEP 5.2 завершен, STEP 5.3 завершен, старый код закомментирован)

**Приоритет:** 🟡 СРЕДНИЙ (изолированные функции, средний риск)

---

### 🔄 6. Модуль "cart/cartTabs.js" (управление вкладками) - **В ПРОЦЕССЕ**

- ✅ `switchCartTab(tabName)` — переключение основных вкладок (~58 строк)
  - Используется в: `cart.js` (setupCartButton, setupCartModal, updateCartTabsVisibility)
  - ✅ Функция вынесена в модуль `cart/cartTabs.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)
- ✅ `switchCartSubtab(subtabName)` — переключение подвкладок (~73 строки)
  - Используется в: `cart.js` (setupCartModal), `cart/cartTabs.js` (switchCartTab)
  - ✅ Функция вынесена в модуль `cart/cartTabs.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)
- ✅ `updateCartTabsVisibility()` — обновление видимости вкладок (~121 строка)
  - Используется в: `cart.js` (setupCartButton, setupCartModal)
  - ✅ Функция вынесена в модуль `cart/cartTabs.js`
  - ✅ Все использования заменены на импортированную версию
  - 🔄 Старый код закомментирован, ожидает проверки (1-2 дня)

**Статус:** 🔄 **В ПРОЦЕССЕ** (STEP 6.1 завершен, STEP 6.2 завершен, STEP 6.3 завершен, старый код закомментирован)

**Приоритет:** 🟢 НИЗКИЙ (изолированные функции, низкий риск)

---

### ⏳ 7. Модуль "cart/cartInit.js" (инициализация корзины) - **НЕ НАЧАТО**

- ⏳ `initCart()` — инициализация корзины (~40 строк)
- ⏳ `setupCartButton()` — настройка кнопки корзины (~28 строк)
- ⏳ `setupCartModal()` — настройка модального окна (~65 строк)

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟢 НИЗКИЙ (изолированные функции, низкий риск)

---

### ⏳ 8. Модуль "cart/cartUI.js" (обновление UI корзины) - **НЕ НАЧАТО**

- ⏳ `fetchActiveReservations()` — получение активных резерваций (~15 строк)
- ⏳ `fetchActiveOrders()` — получение активных заказов (~10 строк)
- ⏳ `fetchActivePurchases()` — получение активных продаж (~12 строк)
- ⏳ `checkHistoryExists()` — проверка наличия истории (~40 строк)
- ⏳ `updateCartButtonVisibility(totalItems, hasHistory)` — обновление видимости кнопки (~30 строк)
- ⏳ `updateCartUI()` — обновление UI корзины (рефакторинг, ~166 строк)

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🔴 ВЫСОКИЙ (критичная функция, высокий риск - делать последним!)

---

## Итого:

- **Всего функций:** ~20
- **Приблизительный объем кода для выгрузки:** ~1500+ строк
- **Текущий размер файла:** ~1973 строки
- **После выгрузки останется:** ~400–500 строк (координация модулей, экспорты)

---

## Рекомендуемый порядок выгрузки:

1. 🔄 **utils/priceUtils.js** - В ПРОЦЕССЕ (STEP 1.1) ✅ Функция вынесена, старый код закомментирован
2. ✅ **utils/imageUtils.js** - ЗАВЕРШЕНО (STEP 2.1, 2.2) ✅ Функции вынесены, протестированы, старый код закомментирован
3. 🔄 **utils/dateUtils.js** - В ПРОЦЕССЕ (STEP 3.1, 3.2) ✅ Функции вынесены, старый код закомментирован
4. 🔄 **cart/cartHistory.js** - В ПРОЦЕССЕ (STEP 4.1, 4.2, 4.3) ✅ Функции loadReservationsHistory(), loadOrdersHistory() и loadPurchasesHistory() вынесены, старый код закомментирован
5. 🔄 **cart/cartActive.js** - В ПРОЦЕССЕ (STEP 5.1 завершен, STEP 5.2 завершен, STEP 5.3 завершен) ✅ Функции loadCart(), loadOrders() и loadPurchases() вынесены, старый код закомментирован
6. 🔄 **cart/cartTabs.js** - В ПРОЦЕССЕ (STEP 6.1 завершен, STEP 6.2 завершен, STEP 6.3 завершен) ✅ Функции switchCartTab(), switchCartSubtab() и updateCartTabsVisibility() вынесены, старый код закомментирован
7. ⏳ **cart/cartInit.js** - НЕ НАЧАТО (STEP 7.1)
8. ⏳ **cart/cartUI.js** - НЕ НАЧАТО (STEP 8.1) ⚠️ **ОСТОРОЖНО!**

---

## Легенда статусов:

- ✅ **ВЫПОЛНЕНО** - Функция выгружена, протестирована, старый код удален
- 🔄 **В ПРОЦЕССЕ** - Частично выполнено, старый код закомментирован
- ⏳ **НЕ НАЧАТО** - Ожидает выполнения

---

## Примечания:

- Рефакторинг проводится мягко и постепенно, по одной функции
- Старый код сначала закомментирован для проверки работоспособности
- После подтверждения работоспособности (через 1-2 дня) старый код удаляется
- Каждый шаг тестируется отдельно перед переходом к следующему
- Критичные функции (updateCartUI) рефакторятся в последнюю очередь

---

## Шаблон для каждого шага:

### Перед началом:
```javascript
// TODO: REFACTORING STEP X.Y - [Название шага]
// Дата начала: YYYY-MM-DD
// Статус: В процессе / Завершен / Откат
```

### В процессе рефакторинга:
```javascript
// ========== REFACTORING STEP X.Y: [Название] ==========
// НОВЫЙ КОД (используется сейчас)
import { newFunction } from './newModule.js';
const result = newFunction(params);

// СТАРЫЙ КОД (закомментирован, будет удален после проверки)
/*
const result = (function() {
    // старая реализация
})();
*/
// ========== END REFACTORING STEP X.Y ==========
```

### После проверки (через 1-2 дня):
- Если все работает: удалить закомментированный код
- Если есть проблемы: откатить изменения

---

## Чеклист безопасности для каждого шага:

### Перед началом:
- [ ] Создан git commit с текущим состоянием
- [ ] Работа в отдельной ветке
- [ ] Прочитан план для этого шага

### После изменений:
- [ ] Проверено в браузере
- [ ] Нет ошибок в консоли
- [ ] Протестированы все сценарии
- [ ] Создан git commit с описанием

### Через 1-2 дня:
- [ ] Все еще работает корректно
- [ ] Удален закомментированный код
- [ ] Создан финальный commit

---

## Оценка времени:

- **Этап 1 (utils/priceUtils.js):** 2-3 часа
- **Этап 2 (utils/imageUtils.js):** 3-4 часа
- **Этап 3 (utils/dateUtils.js):** 2-3 часа
- **Этап 4 (cart/cartHistory.js):** 4-6 часов
- **Этап 5 (cart/cartActive.js):** 4-6 часов
- **Этап 6 (cart/cartTabs.js):** 2-3 часа
- **Этап 7 (cart/cartInit.js):** 2-3 часа
- **Этап 8 (cart/cartUI.js):** 4-8 часов (с тестированием)

**Общее время:** 23-36 часов работы

---

## Риски и митигация:

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Сломанная функциональность корзины | Средняя | Высокое | Сохранение старого кода, постепенный переход |
| Проблемы с импортами | Низкая | Среднее | Проверка путей импорта, использование относительных путей |
| Конфликты в git | Низкая | Среднее | Работа в отдельной ветке, частые коммиты |
| Потеря производительности | Низкая | Низкое | Профилирование после рефакторинга |

