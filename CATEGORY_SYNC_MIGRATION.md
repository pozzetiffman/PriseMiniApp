# MIGRATION-СКРИПТ: Исправление существующих товаров с нарушением инварианта

**Файл:** `backend/fix_category_sync.py` (обновлен)

**Статус:** ✅ Готов к использованию

---

## 📋 ОПИСАНИЕ

Migration-скрипт для исправления существующих товаров с нарушением инварианта `product.bot_id === category.bot_id`.

**Логика:**
1. Находит все товары пользователя
2. Для каждого товара проверяет инвариант
3. Если инвариант нарушен - исправляет через `normalize_category_id`
4. Выводит отчет о результатах

**Безопасность:**
- Работает транзакционно (rollback при ошибке)
- Поддерживает dry-run режим
- Не удаляет данные
- Подробное логирование

---

## 🔧 ИСПОЛЬЗОВАНИЕ

### Проверка проблем (dry-run):
```bash
cd backend
python fix_category_sync.py --dry-run
```

### Исправление для всех пользователей:
```bash
cd backend
python fix_category_sync.py
```

### Исправление для конкретного пользователя:
```bash
cd backend
python fix_category_sync.py --user-id 123456789
```

---

## 📊 ОТЧЕТ

Скрипт выводит:
- Количество найденных товаров
- Количество исправленных товаров
- Количество пропущенных товаров (уже правильно)
- Количество ошибок (категория не найдена)

---

## ⚠️ ВАЖНО

- Скрипт работает транзакционно
- В режиме dry-run изменения не сохраняются
- Без dry-run изменения коммитятся в БД
- Скрипт не удаляет данные
- Если категория не найдена - товар остается без категории (category_id=None)
