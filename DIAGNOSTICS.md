# 🔍 ИНСТРУКЦИЯ ПО ДИАГНОСТИКЕ ПРОБЛЕМ

## Быстрая диагностика проблемы загрузки товаров

### Шаг 1: Проверка Backend

```bash
# Проверьте, что backend запущен
curl http://localhost:8000/api/health
# Должно вернуть: {"status":"ok"}
```

**Если не работает:**
- Запустите backend: `cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`
- Проверьте, что порт 8000 свободен

---

### Шаг 2: Проверка Ngrok

```bash
# Проверьте, что ngrok активен
# В терминале ngrok должен быть виден HTTPS URL
```

**Если не работает:**
- Запустите ngrok: `ngrok http 8000`
- Скопируйте HTTPS URL (например: `https://xxxx-xx-xx-xx-xx.ngrok-free.app`)

---

### Шаг 3: Проверка URL в коде

Откройте `webapp/js/api.js` и проверьте строку 5:

```javascript
export const API_BASE = "https://ВАШ_NGROK_URL.ngrok-free.dev".trim();
```

**URL должен совпадать с ngrok URL из шага 2!**

**Если не совпадает:**
1. Обновите URL в `webapp/js/api.js`
2. Если используете Vercel, задеплойте заново: `cd webapp && vercel --prod`

---

### Шаг 4: Проверка в Telegram Mini App

1. Откройте приложение через Telegram бота (НЕ в браузере!)
2. При загрузке должно появиться сообщение о проверке API
3. Если API недоступен, появится alert с ошибкой

**Что проверить:**
- Приложение открыто через бота (не в браузере)
- Появляется проверка доступности API при запуске
- Если есть ошибка, она отображается в alert

---

### Шаг 5: Проверка логов Backend

В терминале backend должны появиться логи:

```
DEBUG: get_products called with user_id=...
DEBUG: Found X products for user ...
DEBUG: Returning X products
```

**Если логов нет:**
- Запросы не доходят до backend
- Проблема в URL или сети
- Проверьте шаги 1-3

---

### Шаг 6: Проверка ошибок в коде

**Типичные ошибки:**

1. **"Не удалось подключиться к серверу"**
   - Backend не запущен или ngrok не активен
   - URL в api.js не совпадает с ngrok URL

2. **"Превышено время ожидания"**
   - Медленное соединение
   - Backend перегружен
   - Увеличьте таймаут в коде (по умолчанию 10 секунд)

3. **"Telegram WebApp недоступен"**
   - Приложение открыто не через Telegram бота
   - Откройте через бота: `/start` → кнопка витрины

4. **"CORS policy error"**
   - Проблема с настройками CORS на backend
   - Проверьте `backend/app/main.py` - должно быть `allow_origins=["*"]`

---

## Автоматическая диагностика

При запуске приложения теперь выполняется автоматическая проверка:

1. ✅ Инициализация Telegram WebApp
2. ✅ Проверка доступности Telegram
3. ✅ Проверка доступности API (новое!)
4. ✅ Загрузка контекста
5. ✅ Загрузка данных

Если какая-то проверка не проходит, приложение покажет понятное сообщение об ошибке.

---

## Что делать, если проблема сохраняется

1. **Проверьте все шаги выше**
2. **Проверьте логи backend** - должны быть запросы
3. **Проверьте ngrok** - должен быть активен
4. **Проверьте URL** - должен совпадать в api.js и ngrok
5. **Перезапустите все компоненты** в правильном порядке:
   - Backend
   - Ngrok
   - Бот
   - Фронтенд (если используете Vercel)

---

## Контакты для поддержки

Если проблема не решается:
1. Соберите логи backend
2. Сделайте скриншот ошибки в Telegram Mini App
3. Проверьте, что все компоненты запущены

---

**Последнее обновление:** После исправлений в AUDIT_REPORT.md



