# Аудит отображения изображений в Telegram Mini App

## 🔍 Проблема

Изображения товаров не отображаются в карточках товаров, хотя бекенд передает правильные URL.

## 🎯 Причина

Telegram WebView **блокирует ngrok-домены** для прямых запросов через `<img src>`. Это связано с:
- Нестабильной SSL-цепочкой ngrok
- Динамическими доменами
- Политикой безопасности Telegram WebView

## ✅ Решение

Реализована загрузка изображений через **fetch + blob URL** для обхода блокировки:

1. **Карточки товаров** (`webapp/js/app.js:341-437`)
   - Загрузка через `fetch()` с заголовком `ngrok-skip-browser-warning`
   - Преобразование ответа в `blob`
   - Создание `blob URL` через `URL.createObjectURL()`
   - Использование `blob URL` для `img.src`

2. **Модальное окно товара** (`webapp/js/app.js:685-750`)
   - Аналогичный подход с blob URL
   - Очистка blob URL при смене изображения

3. **Корзина** (`webapp/js/cart.js:235-280`)
   - Загрузка изображений через fetch + blob URL
   - Placeholder во время загрузки

## 🔧 Технические детали

### Backend
- Endpoint `/api/images/{filename}` проксирует изображения из `/static/uploads/`
- Функция `make_full_url()` преобразует `/static/uploads/` → `/api/images/`
- Заголовки CORS настроены правильно

### Frontend
- Все изображения загружаются через `fetch()` с заголовком `ngrok-skip-browser-warning: 69420`
- Blob URL создается локально, что обходит блокировку Telegram WebView
- Обработка ошибок с fallback на placeholder

## 📝 Изменения в коде

### `webapp/js/app.js`
- Функция `renderProducts()`: загрузка через fetch + blob URL
- Функция `showModalImage()`: загрузка через fetch + blob URL

### `webapp/js/cart.js`
- Обновлена логика отображения изображений в корзине

## 🧪 Тестирование

1. Откройте приложение в Telegram Mini App
2. Проверьте отображение изображений в:
   - Карточках товаров
   - Модальном окне товара
   - Корзине
3. Проверьте консоль на наличие ошибок загрузки

## ⚠️ Важные замечания

1. **Blob URL очистка**: Blob URL автоматически очищается браузером при закрытии страницы, но для модального окна добавлена ручная очистка при смене изображения.

2. **Производительность**: Загрузка через fetch может быть немного медленнее, чем прямая загрузка, но это необходимо для обхода блокировки.

3. **Альтернативы**: Если проблема сохраняется, можно рассмотреть:
   - Использование стабильного домена вместо ngrok
   - Загрузку изображений через Telegram Bot API (если доступно)
   - Использование CDN для изображений

## 🔗 Связанные файлы

- `backend/app/main.py` - endpoint `/api/images/`
- `backend/app/routers/products.py` - функция `make_full_url()`
- `webapp/js/app.js` - рендеринг товаров и модального окна
- `webapp/js/cart.js` - отображение корзины

