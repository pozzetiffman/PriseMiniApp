# План рефакторинга webapp/js/products.js

## Статус рефакторинга

**Текущий размер файла:** ~1434 строк  
**Количество функций:** 6  
**Целевой размер:** ~50–100 строк (только инициализация, экспорты и импорты)  
**Планируется выгрузить:** ~1300+ строк (рендеринг, модальные окна, навигация, утилиты)  
**Прогресс:** ⏳ Не начато

---

## Список функций для выгрузки из webapp/js/products.js

### 🔄 1. Модуль "webapp/js/utils/products_utils.js" (утилиты) - **В ПРОЦЕССЕ**

- 🔄 `isMobileDevice()` — детекция мобильного устройства (~13 строк) - **В ПРОЦЕССЕ**
  - Используется в: `renderProducts()`, `showModalImage()`
  - Приоритет: 🟢 НИЗКИЙ (простая утилита, низкий риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/utils/products_utils.js`, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ**

**Приоритет:** 🟢 НИЗКИЙ (простые утилиты, низкий риск - можно делать рано)

---

### 🔄 2. Модуль "webapp/js/handlers/products_render.js" (рендеринг товаров) - **В ПРОЦЕССЕ**

- 🔄 `renderProducts(products)` — рендеринг товаров в сетке (~516 строк) - **В ПРОЦЕССЕ**
  - Используется в: вызывается из других модулей (app.js)
  - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга с изображениями, высокий риск - делать очень осторожно!)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/products_render.js`, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ**

**Приоритет:** 🔴 ВЫСОКИЙ (очень большая функция с DOM манипуляциями и загрузкой изображений, высокий риск)

---

### 🔄 3. Модуль "webapp/js/handlers/products_modal.js" (модальное окно товара) - **В ПРОЦЕССЕ**

- 🔄 `showProductModal(prod, finalPrice, fullImages)` — показ модального окна товара (~436 строк) - **В ПРОЦЕССЕ**
  - Используется в: `renderProducts()` (onclick на карточке товара)
  - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга модального окна, высокий риск - делать очень осторожно!)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/products_modal.js`, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ**

**Приоритет:** 🔴 ВЫСОКИЙ (очень большая функция с DOM манипуляциями и сложной логикой модального окна, высокий риск)

---

### 🔄 4. Модуль "webapp/js/handlers/products_modal_image.js" (изображения в модальном окне) - **В ПРОЦЕССЕ**

- 🔄 `showModalImage(index)` — показ изображения в модальном окне (~217 строк) - **В ПРОЦЕССЕ**
  - Используется в: `showProductModal()`, `updateImageNavigation()`
  - Приоритет: 🟡 СРЕДНИЙ (большая функция, сложная логика загрузки изображений через fetch/blob, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/products_modal_image.js`, старый код закомментирован, ожидает проверки

- 🔄 `updateImageNavigation()` — обновление навигации по фото (~198 строк) - **В ПРОЦЕССЕ**
  - Используется в: `showModalImage()`
  - Приоритет: 🟡 СРЕДНИЙ (большая функция, DOM манипуляции для навигации, средний риск)
  - Дата начала: 2024-12-19
  - Статус: Функция выгружена в `webapp/js/handlers/products_modal_image.js`, старый код закомментирован, ожидает проверки

**Статус:** 🔄 **В ПРОЦЕССЕ**

**Приоритет:** 🟡 СРЕДНИЙ (большие функции с DOM манипуляциями и загрузкой изображений, средний риск)

---

## Итого:

- **Всего функций:** 6
- **Приблизительный объем кода для выгрузки:** ~1300+ строк
- **Текущий размер файла:** ~1434 строк
- **После удаления закомментированного кода останется:** ~50–100 строк (инициализация, экспорты, импорты)
- **Созданные модули:**
  - 🔄 `webapp/js/utils/products_utils.js` - утилиты
  - 🔄 `webapp/js/handlers/products_render.js` - рендеринг товаров
  - 🔄 `webapp/js/handlers/products_modal.js` - модальное окно товара
  - 🔄 `webapp/js/handlers/products_modal_image.js` - изображения в модальном окне

---

## Рекомендуемый порядок выгрузки:

1. 🔄 **webapp/js/utils/products_utils.js** - В ПРОЦЕССЕ (STEP 1) - Базовые утилиты
   - Приоритет: 🟢 НИЗКИЙ (простые утилиты, низкий риск - можно делать рано)
   - Статус: STEP 1.1 (isMobileDevice) - функция выгружена, старый код закомментирован, ожидает проверки

2. 🔄 **webapp/js/handlers/products_modal_image.js** - В ПРОЦЕССЕ (STEP 2) - Изображения в модальном окне
   - Приоритет: 🟡 СРЕДНИЙ (большие функции с DOM манипуляциями и загрузкой изображений, средний риск)
   - Статус: STEP 2.1 (showModalImage) и STEP 2.2 (updateImageNavigation) - функции выгружены, старый код закомментирован, ожидает проверки

3. 🔄 **webapp/js/handlers/products_modal.js** - В ПРОЦЕССЕ (STEP 3) - Модальное окно товара
   - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга модального окна, высокий риск - делать очень осторожно!)
   - Статус: STEP 3.1 (showProductModal) - функция выгружена, старый код закомментирован, ожидает проверки

4. 🔄 **webapp/js/handlers/products_render.js** - В ПРОЦЕССЕ (STEP 4) - Рендеринг товаров
   - Приоритет: 🔴 ВЫСОКИЙ (очень большая функция, сложная логика рендеринга с изображениями, высокий риск - делать очень осторожно!)
   - Статус: STEP 4.1 (renderProducts) - функция выгружена, старый код закомментирован, ожидает проверки

---

## Легенда статусов:

- ✅ **ВЫПОЛНЕНО** - Функция выгружена, протестирована, старый код удален
- 🔄 **В ПРОЦЕССЕ** - Частично выполнено, старый код закомментирован
- ⏳ **НЕ НАЧАТО** - Ожидает выполнения

---

## Примечания:

- Рефакторинг проводится мягко и постепенно, по одной функции
- Старый код сначала закомментирован для проверки работоспособности
- После подтверждения работоспособности (через 1-2 дня) старый код удаляется
- Каждый шаг тестируется отдельно перед переходом к следующему
- Критичные функции (renderProducts, showProductModal) рефакторятся осторожно
- Функции с DOM манипуляциями требуют особого внимания
- Тесты в браузере не делаем - инициализация проходит через Telegram ID
- Все необходимые логи только в терминалах (console.log)

---

## Шаблон для каждого шага:

### Перед началом:
```javascript
// TODO: REFACTORING STEP X.Y - [Название шага]
// Дата начала: YYYY-MM-DD
// Статус: В процессе / Завершен / Откат
```

### В процессе рефакторинга:
```javascript
// ========== REFACTORING STEP X.Y: [Название] ==========
// НОВЫЙ КОД (используется сейчас)
import { newFunction } from './handlers/new_module.js';
const result = await newFunction(params);

// СТАРЫЙ КОД (закомментирован, будет удален после проверки)
/*
async function oldFunction(params) {
    // старая реализация
}
*/
// ========== END REFACTORING STEP X.Y ==========
```

### После проверки (через 1-2 дня):
- Если все работает: удалить закомментированный код
- Если есть проблемы: откатить изменения

---

## Чеклист безопасности для каждого шага:

### Перед началом:
- [ ] Создан git commit с текущим состоянием
- [ ] Работа в отдельной ветке
- [ ] Прочитан план для этого шага
- [ ] Понятны зависимости функции (что она использует, где используется)
- [ ] Проверены импорты и экспорты

### После изменений:
- [ ] Проверено через Telegram Mini App
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] Нет ошибок в логах терминала
- [ ] Протестированы все сценарии использования функции
- [ ] Импорты корректны (относительные пути)
- [ ] Экспорты корректны (export function/const)
- [ ] Создан git commit с описанием

### Через 1-2 дня:
- [ ] Все еще работает корректно
- [ ] Удален закомментированный код
- [ ] Создан финальный commit

---

## Оценка времени:

- **Этап 1 (products_utils.js):** 0.5-1 час
- **Этап 2 (products_modal_image.js):** 3-4 часа (большие функции с загрузкой изображений)
- **Этап 3 (products_modal.js):** 4-6 часов (очень большая функция, сложная логика модального окна)
- **Этап 4 (products_render.js):** 5-7 часов (очень большая функция, сложная логика рендеринга с изображениями)

**Общее время:** 12.5-18 часов работы

---

## Риски и митигация:

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Сломанная функциональность UI | Средняя | Высокое | Сохранение старого кода, постепенный переход, тестирование каждого шага |
| Проблемы с импортами/экспортами | Средняя | Среднее | Проверка путей импорта, использование относительных путей, тестирование |
| Конфликты в git | Низкая | Среднее | Работа в отдельной ветке, частые коммиты |
| Потеря производительности | Низкая | Низкое | Профилирование после рефакторинга |
| Проблемы с DOM манипуляциями | Средняя | Высокое | Особое внимание к функциям рендеринга, тестирование всех сценариев |
| Проблемы с загрузкой изображений (fetch/blob) | Средняя | Высокое | Особое внимание к функциям загрузки изображений, тестирование на мобильных устройствах |
| Проблемы с глобальными переменными | Средняя | Среднее | Проверка зависимостей от глобальных переменных (modalElement, modalState, productsGridElement и т.д.) |

---

## Структура после рефакторинга:

```
webapp/js/
├── products.js                    # ~50-100 строк (только инициализация, экспорты, импорты)
├── PRODUCTS_REFACTORING.md       # Этот файл
├── handlers/
│   ├── products_render.js        # Рендеринг товаров (renderProducts)
│   ├── products_modal.js         # Модальное окно товара (showProductModal)
│   └── products_modal_image.js  # Изображения в модальном окне (showModalImage, updateImageNavigation)
└── utils/
    └── products_utils.js         # Утилиты (isMobileDevice)
```

---

## Важные замечания:

1. **Глобальные переменные:** `productsGridElement`, `appContextGetter`, `modalElement`, `modalState`, `loadDataCallback`, `showEditProductModalCallback`, `markAsSoldCallback`, `deleteProductCallback`, `cancelReservationCallback`, `showPurchaseModalCallback`, `showReservationModalCallback`, `showOrderModalCallback` должны быть доступны в модулях через экспорт/импорт или передаваться как параметры

2. **Импорты:** Все обработчики должны импортировать необходимые зависимости из соответствующих модулей (например, API функции из `api.js`, утилиты из `utils/`)

3. **Экспорты:** Функции, используемые в других модулях, должны быть экспортированы (`export function` или `export async function`)

4. **Зависимости от API:** Все функции используют API функции из `api.js` - нужно убедиться, что импорты корректны

5. **Тестирование:** Каждый шаг должен быть протестирован через Telegram Mini App перед переходом к следующему

6. **Логирование:** Использовать `console.log()` для логирования в консоль браузера (не делаем тесты в браузере)

7. **DOM манипуляции:** Функции с большим количеством DOM манипуляций (renderProducts, showProductModal, showModalImage, updateImageNavigation) требуют особого внимания - нужно проверить, что все элементы создаются корректно

8. **Загрузка изображений:** Функции `renderProducts()` и `showModalImage()` работают с изображениями через fetch и blob URLs для обхода блокировки Telegram WebView - нужно убедиться, что эта логика работает корректно после рефакторинга

9. **Обработчики событий:** Многие функции устанавливают обработчики событий (onclick, onchange, ontouchstart, ontouchend и т.д.) - нужно убедиться, что они работают после рефакторинга

10. **Зависимости от window:** Некоторые функции могут использовать глобальные функции - нужно убедиться, что эти зависимости доступны после рефакторинга

11. **Зависимости от других модулей:** Функции используют функции из других модулей (`getCurrentShopSettings` из `admin.js`, `getProductPriceDisplay` из `utils/priceUtils.js`, `toggleHotOffer`, `trackShopVisit` из `api.js`, `getTelegramInstance` из `telegram.js`) - нужно убедиться, что импорты корректны

12. **Инициализация зависимостей:** Функция `initProductsDependencies()` устанавливает зависимости для всех функций - нужно убедиться, что эта система работает после рефакторинга

13. **Модальное окно:** Функции `showProductModal()`, `showModalImage()`, `updateImageNavigation()` работают с модальным окном через `modalElement` и `modalState` - нужно убедиться, что эта система работает после рефакторинга

14. **Рендеринг изображений:** Функция `renderProducts()` создает карточки товаров с изображениями, используя сложную логику загрузки через fetch/blob для мобильных устройств - нужно убедиться, что эта логика работает корректно после рефакторинга

15. **Навигация по фото:** Функция `updateImageNavigation()` создает навигацию по фото в модальном окне с обработчиками свайпов для мобильных устройств - нужно убедиться, что эта логика работает корректно после рефакторинга
