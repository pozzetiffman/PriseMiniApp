# 🔍 АУДИТ ФУНКЦИОНАЛЬНОСТИ ПОКУПОК

**Дата аудита:** 2024  
**Проект:** PriseMiniApp (Telegram Mini App)  
**Условие:** Проект работает **ИСКЛЮЧИТЕЛЬНО** в Telegram Mini App (WebView) + HTTPS

---

## ✅ ЧТО СДЕЛАНО

### 1. **Модель базы данных** ✅
- **Файл:** `backend/app/db/models.py`
- **Модель:** `Purchase` (строки 138-162)
- **Поля:**
  - `id`, `product_id`, `user_id`, `purchased_by_user_id`
  - `created_at`, `is_completed`, `is_cancelled`, `status`
  - `first_name`, `last_name`, `middle_name`
  - `phone_number`, `city`, `address`, `notes`
  - `payment_method`, `organization`
  - `images_urls` (JSON массив, до 5 фото)
  - `video_url` (1 видео)
- **Связи:** `product` (relationship с Product)

### 2. **Миграция базы данных** ✅
- **Файл:** `backend/migrate_add_purchases.py`
- **Статус:** Создана и готова к выполнению
- **Создает:** Таблицу `purchases` с индексами

### 3. **API схемы (Pydantic)** ✅
- **Файл:** `backend/app/models/purchase.py`
- **Схемы:**
  - `PurchaseBase` - базовые поля
  - `PurchaseCreate` - для создания (с `images_urls`, `video_url`)
  - `Purchase` - полная модель с `product` (ProductInfo)
  - `PurchaseUpdate` - для обновления статуса
  - `ProductInfo` - информация о товаре в покупке

### 4. **API Endpoints** ✅
- **Файл:** `backend/app/routers/purchases.py`
- **Роутер подключен:** `backend/app/main.py` (строка 90)

#### Реализованные endpoints:
1. **POST `/api/purchases/`** ✅
   - Создание заявки на покупку
   - Загрузка изображений (до 5 шт) и видео (1 шт)
   - Валидация `is_for_sale = true`
   - Отправка уведомления владельцу магазина
   - Преобразование URL в полные HTTPS

2. **GET `/api/purchases/my`** ✅
   - Получение покупок текущего пользователя (как покупатель)
   - Фильтрация по `purchased_by_user_id`
   - Сортировка по `created_at DESC`

3. **GET `/api/purchases/all?user_id={id}`** ✅
   - Получение всех покупок для владельца магазина
   - Проверка прав доступа (`viewer_id == user_id`)
   - Фильтрация по `user_id` (владелец магазина)

4. **PATCH `/api/purchases/{purchase_id}?user_id={id}`** ✅
   - Обновление статуса покупки
   - Поля: `is_completed`, `is_cancelled`, `status`
   - Проверка прав доступа

### 5. **Фронтенд - API функции** ✅
- **Файл:** `webapp/js/api.js`
- **Функции:**
  - `createPurchaseAPI(productId, formData)` - создание заявки (строки 926-954)
  - `getMyPurchasesAPI()` - получение моих покупок (строки 957-984)
  - `getAllPurchasesAPI(shopOwnerId)` - получение всех покупок для админа (строки 987-1014)

### 6. **Фронтенд - UI товаров** ✅
- **Файл:** `webapp/js/app.js`
- **Логика:**
  - Кнопка "🛒 Продать" для товаров с `is_for_sale = true` (строки 1678-1684)
  - Скрытие кнопок резервации/заказа для таких товаров
  - Функция `showPurchaseModal(prod)` (строки 3505-3602)
  - Функция `submitPurchaseForm(productId)` (строки 3605-3673)

### 7. **Фронтенд - Модальное окно покупки** ✅
- **Файл:** `webapp/index.html` (строки 164-224)
- **Поля формы:**
  - ФИО (фамилия*, имя*, отчество)
  - Телефон* (tel)
  - Город* (text)
  - Адрес* (textarea)
  - Примечание (textarea)
  - Форма оплаты* (radio: cash/bank_transfer)
  - Организация (text)
  - Фото (file, multiple, до 5 шт)
  - Видео (file, 1 шт)
- **Валидация:** Обязательные поля отмечены `*`
- **Предпросмотр:** Изображений и видео перед отправкой

---

## ❌ ЧТО ОСТАЛОСЬ СДЕЛАТЬ

### 1. **Корзина - Вкладка "Покупки"** ❌

#### Текущее состояние:
- **Файл:** `webapp/index.html` (строки 234-243)
- **Вкладки:** Только "🔒 Резервации" и "🛒 Заказы"
- **Отсутствует:** Вкладка "💰 Покупки"

#### Что нужно сделать:

##### 1.1. HTML (index.html)
```html
<!-- Добавить вкладку в корзину -->
<div class="cart-tabs">
    <button class="cart-tab active" data-tab="reservations">🔒 Резервации</button>
    <button class="cart-tab" data-tab="orders">🛒 Заказы</button>
    <button class="cart-tab" data-tab="purchases">💰 Покупки</button>
</div>

<!-- Добавить контейнер для покупок -->
<div id="purchases-items" class="cart-items" style="display: none;">
    <!-- Покупки будут здесь -->
</div>
```

##### 1.2. JavaScript (cart.js)
- **Функция `switchCartTab(tabName)`** - добавить обработку `'purchases'`
- **Функция `loadPurchases()`** - новая функция для загрузки покупок:
  - Вызов `getMyPurchasesAPI()`
  - Отображение списка покупок с:
    - Изображением товара
    - Названием товара
    - Статусом (pending/completed/cancelled)
    - Датой создания
    - Информацией о заявке (ФИО, телефон, город)
    - Превью фото/видео (если есть)
- **Функция `updateCartUI()`** - добавить подсчет покупок в `totalItems`
- **Импорт:** Добавить `getMyPurchasesAPI` в импорты

##### 1.3. CSS (style.css)
- Стили для `.cart-tab` уже есть (должны работать)
- Стили для `.cart-item` уже есть (должны работать)
- Возможно, добавить специфичные стили для статусов покупок

---

### 2. **Админка - Вкладка "Покупки"** ❌

#### Текущее состояние:
- **Файл:** `webapp/js/admin.js`
- **Вкладки админки:** "⚙️ Настройки", "🛒 Заказы", "✅ Проданные", "📊 Статистика"
- **Отсутствует:** Вкладка "💰 Покупки"

#### Что нужно сделать:

##### 2.1. HTML (admin.js - createAdminModal)
```javascript
// Добавить вкладку в админку
<button class="admin-tab" data-tab="purchases">
    <span style="font-size: 18px;">💰</span>
    <span>Покупки</span>
</button>

// Добавить контейнер для покупок
<div id="admin-tab-purchases" class="admin-tab-content">
    <div id="purchases-list" class="purchases-list">
        <p class="loading">Загрузка заявок на покупку...</p>
    </div>
</div>
```

##### 2.2. JavaScript (admin.js)
- **Функция `switchAdminTab(tabName)`** - добавить обработку `'purchases'`
- **Функция `loadPurchases()`** - новая функция для загрузки покупок:
  - Получить `shopOwnerId` из контекста
  - Вызов `getAllPurchasesAPI(shopOwnerId)`
  - Отображение списка покупок с:
    - Информацией о товаре (название, цена, изображение)
    - Информацией о покупателе (ФИО, телефон, город, адрес)
    - Формой оплаты
    - Примечанием
    - Организацией (если есть)
    - Превью фото/видео (если есть)
    - Статусом (pending/completed/cancelled)
    - Кнопками управления:
      - ✅ Выполнить (если pending)
      - ❌ Отменить (если pending)
      - Обновить статус
- **Импорт:** Добавить `getAllPurchasesAPI` и функцию обновления статуса в импорты

##### 2.3. API функция обновления статуса
- **Файл:** `webapp/js/api.js`
- **Функция:** `updatePurchaseStatusAPI(purchaseId, shopOwnerId, statusData)`
  - Вызов `PATCH /api/purchases/{purchase_id}?user_id={shopOwnerId}`
  - Передача `PurchaseUpdate` (is_completed, is_cancelled, status)

---

## 📋 ПЛАН РЕАЛИЗАЦИИ

### Приоритет 1: Корзина - Вкладка "Покупки"
1. Обновить HTML корзины (добавить вкладку и контейнер)
2. Добавить функцию `loadPurchases()` в `cart.js`
3. Обновить `switchCartTab()` для обработки `'purchases'`
4. Обновить `updateCartUI()` для подсчета покупок
5. Добавить стили для статусов покупок (опционально)

### Приоритет 2: Админка - Вкладка "Покупки"
1. Добавить вкладку в `createAdminModal()` в `admin.js`
2. Добавить контейнер для покупок
3. Создать функцию `loadPurchases()` в `admin.js`
4. Обновить `switchAdminTab()` для обработки `'purchases'`
5. Добавить функцию `updatePurchaseStatusAPI()` в `api.js`
6. Реализовать UI для управления статусами покупок

---

## 🔍 ПРОВЕРКА СОВМЕСТИМОСТИ С TELEGRAM WEBVIEW

### ✅ Уже реализовано:
- Все API вызовы используют `X-Telegram-Init-Data` заголовок
- Валидация через `validate_init_data_multi_bot()`
- Преобразование относительных URL в полные HTTPS URL
- Загрузка файлов через FormData (поддерживается в WebView)
- Использование `Telegram.WebApp` API (если нужно)

### ⚠️ Требует проверки:
- Отображение изображений/видео из покупок в корзине и админке
- Работа с blob URL для изображений (уже используется в корзине для заказов)
- Открытие ссылок на товары (если нужно)

---

## 📝 ЗАМЕТКИ

1. **Статусы покупок:**
   - `pending` - ожидает обработки
   - `completed` - выполнена
   - `cancelled` - отменена

2. **Права доступа:**
   - Покупатель видит только свои покупки (`/api/purchases/my`)
   - Владелец магазина видит все покупки своих товаров (`/api/purchases/all?user_id={id}`)
   - Обновление статуса доступно только владельцу магазина

3. **Уведомления:**
   - При создании покупки владелец магазина получает уведомление в Telegram
   - Используется токен подключенного бота или основной токен

4. **Файлы:**
   - Изображения сохраняются в `static/uploads/` с UUID именами
   - Видео сохраняются в `static/uploads/` с UUID именами
   - URL преобразуются в полные HTTPS через `make_full_url()`

---

## ✅ ИТОГОВЫЙ СТАТУС

**Выполнено:** 7 из 9 задач (78%)  
**Осталось:** 2 задачи (22%)

### Готово к использованию:
- ✅ Создание заявок на покупку
- ✅ API для получения покупок
- ✅ Модальное окно формы покупки

### Требует доработки:
- ❌ Отображение покупок в корзине клиента
- ❌ Управление покупками в админке

---

**Дата создания отчета:** 2024  
**Версия:** 1.0



