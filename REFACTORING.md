# План рефакторинга app.js

## Статус рефакторинга

**Текущий размер файла:** 2939 строк (после выгрузки категорий и renderProducts)  
**Целевой размер:** ~900–1000 строк  
**Выгружено:** ~530+ строк

---

## Список функций для выгрузки из app.js

### ✅ 1. Модуль "categories.js" (категории) - **ВЫПОЛНЕНО**

- ✅ `renderCategories(categories)` — рендеринг категорий (~475 строк)
- ✅ `initCategoryFilterHandlers(filterDropdown)` — инициализация обработчиков фильтра (~90 строк)
- ✅ `updateCategoryFilter()` — обновление фильтра категорий (~55 строк)
- ✅ `updateCategoryFilterCount()` — обновление счетчика (~15 строк)
- ✅ Переменные состояния: `currentCategoryId`, `selectedCategoryIds`, `allCategories`, `categoriesHierarchy`, `selectedMainCategoryId`

**Статус:** Полностью выгружено, старый код удален

---

### ✅ 2. Модуль "products.js" (работа с товарами) - **ВЫПОЛНЕНО**

- ✅ `renderProducts(products)` — рендеринг товаров (~533 строки) - **ВЫПОЛНЕНО**
- ✅ `showProductModal(prod, finalPrice, fullImages)` — показ модального окна товара (~420 строк) - **ВЫПОЛНЕНО**
- ✅ `showModalImage(index)` — показ изображения в модальном окне (~208 строк) - **ВЫПОЛНЕНО**
- ✅ `updateImageNavigation()` — обновление навигации по фото (~164 строки) - **ВЫПОЛНЕНО**
- ✅ `isMobileDevice()` — детекция устройства (~9 строк) - **ВЫПОЛНЕНО**

**Статус:** Полностью выгружено (5 из 5 функций)

---

### 🔄 3. Модуль "product-edit.js" (редактирование товаров)

- ✅ `showEditProductModal(prod)` — показ модального окна редактирования (~289 строк) - **ВЫПОЛНЕНО** (старый код удалён)
- ⏳ `saveProductEdit(productId)` — сохранение изменений (~202 строки)
- ⏳ `deleteProduct(productId)` — удаление товара (~26 строк)
- ⏳ `markAsSold(productId, product)` — пометить как проданный (~41 строка)
- ⏳ `showSellModal(productId, product)` — модальное окно продажи (~84 строки)

**Статус:** В процессе (1/5 функций)

---

### ⏳ 4. Модуль "reservations.js" (резервации)

- ⏳ `showReservationModal(productId)` — показ модального окна резервации (~122 строки)
- ⏳ `createReservation(productId, hours, quantity)` — создание резервации (~29 строк)
- ⏳ `cancelReservation(reservationId, productId)` — отмена резервации (~30 строк)
- ⏳ `window.cancelReservationFromCart()` — отмена из корзины (~5 строк)
- ⏳ `window.clearReservationsHistory()` — очистка истории (~22 строки)

**Статус:** Не начато

---

### ⏳ 5. Модуль "orders.js" (заказы)

- ⏳ `showOrderModal(productId)` — показ модального окна заказа (~34 строки)
- ⏳ `resetOrderForm()` — сброс формы заказа (~12 строк)
- ⏳ `updateOrderProductSummary(product)` — обновление информации о товаре (~26 строк)
- ⏳ `showOrderStep(step)` — показ шага формы (~15 строк)
- ⏳ `setupOrderFormHandlers(productId)` — настройка обработчиков (~58 строк)
- ⏳ `submitOrder(productId)` — отправка заказа (~47 строк)
- ⏳ `createOrder(productId, quantity)` — создание заказа (legacy, ~4 строки)
- ⏳ `cancelOrder(orderId)` — отмена заказа (~28 строк)
- ⏳ `window.cancelOrderFromCart()` — отмена из корзины (~6 строк)
- ⏳ `window.clearOrdersHistory()` — очистка истории (~22 строки)

**Статус:** Не начато

---

### ⏳ 6. Модуль "purchases.js" (продажи/покупки)

- ⏳ `showPurchaseModal(prod)` — показ модального окна продажи (~98 строк)
- ⏳ `submitPurchaseForm(productId)` — отправка формы продажи (~91 строка)
- ⏳ `cancelPurchase(purchaseId)` — отмена продажи (~28 строк)
- ⏳ `window.cancelPurchaseFromCart()` — отмена из корзины (~6 строк)
- ⏳ `window.clearPurchasesHistory()` — очистка истории (~22 строки)

**Статус:** Не начато

---

### ⏳ 7. Модуль "filters.js" (фильтры товаров)

- ⏳ `initFilters()` — инициализация фильтров (~3 строки)
- ⏳ `updateProductFilterOptions()` — обновление опций фильтра (~93 строки)
- ⏳ `applyFilters()` — применение фильтров (~130 строк)

**Статус:** Не начато

---

### ⏳ 8. Модуль "modals.js" (настройка модальных окон)

- ⏳ `setupModals()` — настройка модальных окон (~138 строк)

**Статус:** Не начато

---

### ⏳ 9. Модуль "data.js" (загрузка данных)

- ⏳ `window.loadData()` — загрузка данных (категории и товары) (~93 строки)
- ⏳ `window.updateShopNameInHeader()` — обновление заголовка (~26 строк)

**Статус:** Не начато

---

## Итого:

- **Всего функций:** ~38
- **Приблизительный объем кода для выгрузки:** ~2500+ строк
- **Текущий размер файла:** 2939 строк
- **После выгрузки останется:** ~900–1000 строк (инициализация, глобальные переменные, координация модулей)

---

## Рекомендуемый порядок выгрузки:

1. ✅ **categories.js** - ВЫПОЛНЕНО
2. 🔄 **products.js** - В ПРОЦЕССЕ (1/5 функций)
3. ⏳ **product-edit.js**
4. ⏳ **reservations.js**
5. ⏳ **orders.js**
6. ⏳ **purchases.js**
7. ⏳ **filters.js**
8. ⏳ **modals.js**
9. ⏳ **data.js**

---

## Легенда статусов:

- ✅ **ВЫПОЛНЕНО** - Функция выгружена, протестирована, старый код удален
- 🔄 **В ПРОЦЕССЕ** - Частично выполнено
- ⏳ **НЕ НАЧАТО** - Ожидает выполнения

---

## Примечания:

- Рефакторинг проводится мягко и постепенно, по одной функции
- Старый код сначала закомментирован для проверки работоспособности
- После подтверждения работоспособности старый код удаляется
- Все зависимости передаются через инициализацию модулей

