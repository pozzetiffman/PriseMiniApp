# 🔧 Устранение неполадок

## ❌ ПРОБЛЕМА 1: Синтаксическая ошибка в backend

**Ошибка:**
```
SyntaxError: 'await' outside async function
```

**Исправлено:** ✅
- Функция `get_user_reservations_legacy` теперь `async`

**Что делать:**
1. Перезапустите backend
2. Ошибка должна исчезнуть

---

## ❌ ПРОБЛЕМА 2: В новом боте нет команд управления

**Симптомы:**
- В новом боте нет меню управления
- Нет команд `/manage`, `/start` и т.д.
- Только кнопка "Открыть магазин"

**Причина:**
- Новый бот не запущен как отдельный процесс
- Команды управления находятся в коде бота (`bot/bot.py`)
- Новый бот только зарегистрирован в БД, но не запущен

**Решение:**

### Вариант 1: Использовать главный бот для управления (РЕКОМЕНДУЕТСЯ)

1. **Управление магазином:** через главный бот
   - Откройте главного бота
   - Используйте `/manage`, `/start` и другие команды

2. **Показ магазина клиентам:** через нового бота
   - Клиенты открывают нового бота
   - Нажимают кнопку "Открыть магазин"
   - WebApp открывается

### Вариант 2: Запустить нового бота как отдельный процесс

Это требует значительной доработки. См. `NEW_BOT_COMMANDS_EXPLANATION.md`

---

## ❌ ПРОБЛЕМА 3: Товары не загружаются в WebApp

**Симптомы:**
- WebApp открывается
- Показывается "Загрузка товаров..."
- Товары не загружаются

**Возможные причины:**

1. **Ошибка авторизации (401):**
   - Проверьте логи backend
   - Убедитесь, что валидация initData работает

2. **Нет товаров в магазине:**
   - Проверьте, что товары добавлены через главный бот
   - Используйте `/manage` в главном боте

3. **Проблема с контекстом:**
   - Проверьте, что `appContext` загружается правильно
   - Проверьте консоль браузера (F12)

**Что проверить:**

1. **Логи backend:**
   - Должны быть запросы к `/api/context`
   - Должны быть запросы к `/api/products/`
   - Не должно быть ошибок 401

2. **Консоль браузера (F12):**
   - Откройте DevTools
   - Проверьте вкладку Console
   - Ищите ошибки

3. **Network (F12):**
   - Проверьте запросы к API
   - Проверьте статус ответов

---

## 🔍 ДИАГНОСТИКА

### Шаг 1: Проверить логи backend

```bash
# В логах должны быть:
✅ Validated initData - user_id=...
📡 GET /api/context - viewer_id=...
✅ Using own shop - shop_owner_id=...
DEBUG: get_products called with user_id=...
```

### Шаг 2: Проверить консоль браузера

1. Откройте WebApp
2. Нажмите F12 (DevTools)
3. Проверьте вкладку Console
4. Ищите ошибки или предупреждения

### Шаг 3: Проверить Network

1. Откройте DevTools (F12)
2. Вкладка Network
3. Проверьте запросы к `/api/context` и `/api/products/`
4. Проверьте статус ответов (должно быть 200)

---

## ✅ БЫСТРОЕ РЕШЕНИЕ

1. **Перезапустите backend:**
   ```bash
   # Остановите (Ctrl+C)
   # Запустите заново:
   cd backend
   source ../.venv/bin/activate
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

2. **Проверьте, что бот зарегистрирован:**
   - В логах должно быть: "✅ Bot registered: ..."

3. **Используйте главный бот для управления:**
   - Откройте главного бота
   - Используйте `/manage` для управления товарами

4. **Протестируйте WebApp:**
   - Откройте нового бота
   - Нажмите "Открыть магазин"
   - Проверьте консоль браузера (F12)

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

- `NEW_BOT_COMMANDS_EXPLANATION.md` - объяснение про команды
- `FIXES_SUMMARY.md` - исправления валидации

---

**Статус**: ✅ **ИСПРАВЛЕНО**

**Следующий шаг**: Перезапустите backend и протестируйте!




