# План рефакторинга webapp/js/api.js

## Статус рефакторинга

**Текущий размер файла:** ~1290 строк  
**Количество функций:** ~50+  
**Целевой размер:** ~50–100 строк (только базовые утилиты, экспорты и импорты)  
**Планируется выгрузить:** ~1200+ строк (API функции, сгруппированные по модулям)  
**Прогресс:** ⏳ Не начато

---

## Список функций для выгрузки из webapp/js/api.js

### 🔄 1. Модуль "webapp/js/api/config.js" (базовые константы и утилиты) - **В ПРОЦЕССЕ**

- ✅ `API_BASE` — базовый URL API (~1 строка) - **ВЫПОЛНЕНО**
  - Используется в: все API функции
  - Приоритет: 🟢 НИЗКИЙ (константа, низкий риск)
  - Статус: Выгружено в `api/config.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

- ✅ `getBaseHeadersNoAuth()` — базовые заголовки без авторизации (~7 строк) - **ВЫПОЛНЕНО**
  - Используется в: `fetchCategories()`, `fetchProducts()`, `cartActive.js`, `cartHistory.js`
  - Приоритет: 🟢 НИЗКИЙ (простая функция, низкий риск)
  - Статус: Выгружено в `api/config.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

- ✅ `getBaseHeaders()` — базовые заголовки с авторизацией (~16 строк) - **ВЫПОЛНЕНО**
  - Используется в: большинство API функций (63+ использования)
  - Приоритет: 🟡 СРЕДНИЙ (критичная функция авторизации, средний риск)
  - Статус: Выгружено в `api/config.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости. Импорт `getInitData` добавлен в `config.js`.

- ✅ `fetchOptions` — опции для fetch (обратная совместимость) (~6 строк) - **ВЫПОЛНЕНО**
  - Используется в: может использоваться в других модулях (для обратной совместимости)
  - Приоритет: 🟢 НИЗКИЙ (константа для обратной совместимости, низкий риск)
  - Статус: Выгружено в `api/config.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

**Статус:** ✅ **ВЫПОЛНЕНО** (Все функции модуля config.js выгружены: API_BASE, getBaseHeadersNoAuth(), getBaseHeaders(), fetchOptions)

**Приоритет:** 🟢 НИЗКИЙ (базовые утилиты, низкий риск - можно делать рано)

---

### ✅ 2. Модуль "webapp/js/api/context.js" (контекст и настройки магазина) - **ВЫПОЛНЕНО**

- ✅ `getContext(shopOwnerId)` — получение контекста магазина (~48 строк) - **ВЫПОЛНЕНО**
  - Используется в: `app.js` (инициализация)
  - Приоритет: 🔴 ВЫСОКИЙ (критичная функция инициализации, высокий риск)
  - Статус: Выгружено в `api/context.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

- ✅ `getShopSettings(shopOwnerId)` — получение настроек магазина (~21 строка) - **ВЫПОЛНЕНО**
  - Используется в: `admin.js`, `data.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в нескольких местах, средний риск)
  - Статус: Выгружено в `api/context.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

- ✅ `updateShopSettings(settingsUpdate)` — обновление настроек магазина (~26 строк) - **ВЫПОЛНЕНО**
  - Используется в: `admin_settings.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение настроек, средний риск)
  - Статус: Выгружено в `api/context.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

**Статус:** ✅ **ВЫПОЛНЕНО** (Все функции модуля context.js выгружены: getContext, getShopSettings, updateShopSettings)

**Приоритет:** 🔴 ВЫСОКИЙ (критичные функции инициализации, высокий риск - делать осторожно)

---

### 🔄 3. Модуль "webapp/js/api/categories.js" (категории) - **В ПРОЦЕССЕ**

- 🔄 `fetchCategories(shopOwnerId, botId, flat)` — загрузка категорий (~34 строки) - **В ПРОЦЕССЕ**
  - Используется в: `data.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется при загрузке данных, средний риск)
  - Статус: Выгружено в `api/categories.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

**Статус:** 🔄 **В ПРОЦЕССЕ**

**Приоритет:** 🟡 СРЕДНИЙ (функция загрузки данных, средний риск)

---

### 🔄 4. Модуль "webapp/js/api/products_read.js" (чтение товаров) - **В ПРОЦЕССЕ**

- ✅ `fetchProducts(shopOwnerId, categoryId, botId)` — загрузка товаров (~34 строки) - **ВЫПОЛНЕНО**
  - Используется в: `data.js`, `admin_settings.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется при загрузке данных, средний риск)
  - Статус: Выгружено в `api/products_read.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

- 🔄 `getSoldProductsAPI(shopOwnerId)` — получение списка проданных товаров (~24 строки) - **В ПРОЦЕССЕ**
  - Используется в: `admin_sold.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в админке, средний риск)
  - Статус: Выгружено в `api/products_read.js`, старый код закомментирован в `api.js`, добавлен реэкспорт для обратной совместимости

**Статус:** 🔄 **В ПРОЦЕССЕ** (Функции fetchProducts и getSoldProductsAPI выгружены, старый код закомментирован, добавлен реэкспорт для обратной совместимости)

**Приоритет:** 🟡 СРЕДНИЙ (функции чтения данных, средний риск)

---

### ⏳ 5. Модуль "webapp/js/api/products_update.js" (обновление товаров) - **НЕ НАЧАТО**

- ⏳ `toggleHotOffer(productId, shopOwnerId, isHotOffer)` — переключение горящего предложения (~28 строк) - **НЕ НАЧАТО**
  - Используется в: `products_modal.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `updateProductAPI(productId, shopOwnerId, price, discount)` — обновление цены и скидки (~29 строк) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `updateProductNameDescriptionAPI(productId, shopOwnerId, name, description)` — обновление названия и описания (~29 строк) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `updateProductQuantityAPI(productId, shopOwnerId, quantity, quantityUnit)` — обновление количества (~30 строк) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `updateProductMadeToOrderAPI(productId, shopOwnerId, isMadeToOrder)` — обновление статуса "под заказ" (~33 строки) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `updateProductQuantityShowEnabledAPI(productId, shopOwnerId, quantityShowEnabled)` — обновление настройки показа количества (~33 строки) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `updateProductForSaleAPI(productId, shopOwnerId, forSaleData)` — обновление функции "покупка" (~39 строк) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `bulkUpdateAllProductsMadeToOrderAPI(isMadeToOrder)` — массовое обновление статуса "под заказ" (~34 строки) - **НЕ НАЧАТО**
  - Используется в: `admin_settings.js`
  - Приоритет: 🟡 СРЕДНИЙ (массовое изменение товаров, средний риск)
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (функции обновления товаров, средний риск - делать по одной функции)

---

### ⏳ 6. Модуль "webapp/js/api/products_delete.js" (удаление товаров) - **НЕ НАЧАТО**

- ⏳ `deleteProductAPI(productId, shopOwnerId)` — удаление товара (~25 строк) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (удаление товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `markProductSoldAPI(productId, shopOwnerId, quantity)` — пометить товар как проданный (~25 строк) - **НЕ НАЧАТО**
  - Используется в: `product-edit.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение статуса товара, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `deleteSoldProductAPI(soldId, shopOwnerId)` — удалить запись о проданном товаре (~21 строка) - **НЕ НАЧАТО**
  - Используется в: `admin_sold.js`
  - Приоритет: 🟡 СРЕДНИЙ (удаление записи, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `deleteSoldProductsAPI(soldIds, shopOwnerId)` — удалить несколько записей о проданных товарах (~30 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_sold.js`
  - Приоритет: 🟡 СРЕДНИЙ (массовое удаление, средний риск)
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (функции удаления, средний риск - делать по одной функции)

---

### ⏳ 7. Модуль "webapp/js/api/reservations.js" (резервации) - **НЕ НАЧАТО**

- ⏳ `fetchUserReservations()` — загрузка резерваций для корзины (~13 строк) - **НЕ НАЧАТО**
  - Используется в: `cartActive.js`, `cartUI.js`, `cartTabs.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в корзине, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `fetchReservationsHistory()` — загрузка истории резерваций (~17 строк) - **НЕ НАЧАТО**
  - Используется в: `cartHistory.js`, `cartUI.js`, `cartTabs.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в корзине, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `createReservationAPI(productId, hours, quantity)` — создание резервации (~25 строк) - **НЕ НАЧАТО**
  - Используется в: `reservations.js`
  - Приоритет: 🟡 СРЕДНИЙ (создание резервации, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `cancelReservationAPI(reservationId)` — отмена резервации (~24 строки) - **НЕ НАЧАТО**
  - Используется в: `reservations.js`
  - Приоритет: 🟡 СРЕДНИЙ (отмена резервации, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `clearReservationsHistoryAPI()` — очистка истории резерваций (~25 строк) - **НЕ НАЧАТО**
  - Используется в: может использоваться в других модулях
  - Приоритет: 🟢 НИЗКИЙ (очистка истории, низкий риск)
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (функции резерваций, средний риск - делать по одной функции)

---

### ⏳ 8. Модуль "webapp/js/api/orders.js" (заказы) - **НЕ НАЧАТО**

- ⏳ `createOrderAPI(orderData)` — создание заказа (~44 строки) - **НЕ НАЧАТО**
  - Используется в: `orders.js`
  - Приоритет: 🟡 СРЕДНИЙ (создание заказа, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getShopOrdersAPI()` — получение заказов магазина (~16 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_orders.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в админке, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getUserUsernameAPI(userId)` — получение username пользователя (~17 строк) - **НЕ НАЧАТО**
  - Используется в: может использоваться в других модулях
  - Приоритет: 🟢 НИЗКИЙ (получение username, низкий риск)
  - Статус: Ожидает выполнения

- ⏳ `getMyOrdersAPI()` — получение моих заказов (~16 строк) - **НЕ НАЧАТО**
  - Используется в: `cartActive.js`, `cartUI.js`, `cartTabs.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в корзине, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getOrdersHistoryAPI()` — получение истории заказов (~17 строк) - **НЕ НАЧАТО**
  - Используется в: `cartHistory.js`, `cartUI.js`, `cartTabs.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в корзине, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `completeOrderAPI(orderId)` — выполнение заказа (~24 строки) - **НЕ НАЧАТО**
  - Используется в: `admin_orders.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение статуса заказа, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `cancelOrderAPI(orderId)` — отмена заказа (~24 строки) - **НЕ НАЧАТО**
  - Используется в: `orders.js`, `admin_orders.js`
  - Приоритет: 🟡 СРЕДНИЙ (отмена заказа, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `deleteOrderAPI(orderId)` — удаление заказа (~24 строки) - **НЕ НАЧАТО**
  - Используется в: `admin_orders.js`
  - Приоритет: 🟡 СРЕДНИЙ (удаление заказа, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `deleteOrdersAPI(orderIds)` — удаление нескольких заказов (~29 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_orders.js`
  - Приоритет: 🟡 СРЕДНИЙ (массовое удаление, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `clearOrdersHistoryAPI()` — очистка истории заказов (~25 строк) - **НЕ НАЧАТО**
  - Используется в: может использоваться в других модулях
  - Приоритет: 🟢 НИЗКИЙ (очистка истории, низкий риск)
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (функции заказов, средний риск - делать по одной функции)

---

### ⏳ 9. Модуль "webapp/js/api/purchases.js" (покупки) - **НЕ НАЧАТО**

- ⏳ `createPurchaseAPI(productId, formData)` — создание заявки на покупку (~29 строк) - **НЕ НАЧАТО**
  - Используется в: `purchases.js`
  - Приоритет: 🟡 СРЕДНИЙ (создание покупки, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getMyPurchasesAPI()` — получение моих покупок (~27 строк) - **НЕ НАЧАТО**
  - Используется в: `cartActive.js`, `cartUI.js`, `cartTabs.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в корзине, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `cancelPurchaseAPI(purchaseId)` — отмена покупки (~25 строк) - **НЕ НАЧАТО**
  - Используется в: `purchases.js`
  - Приоритет: 🟡 СРЕДНИЙ (отмена покупки, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getPurchasesHistoryAPI()` — получение истории покупок (~28 строк) - **НЕ НАЧАТО**
  - Используется в: `cartHistory.js`, `cartUI.js`, `cartTabs.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в корзине, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getAllPurchasesAPI(shopOwnerId)` — получение всех покупок для админа (~27 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_purchases.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в админке, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `updatePurchaseStatusAPI(purchaseId, shopOwnerId, statusData)` — обновление статуса покупки (~26 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_purchases.js`
  - Приоритет: 🟡 СРЕДНИЙ (изменение статуса покупки, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `clearPurchasesHistoryAPI()` — очистка истории покупок (~25 строк) - **НЕ НАЧАТО**
  - Используется в: может использоваться в других модулях
  - Приоритет: 🟢 НИЗКИЙ (очистка истории, низкий риск)
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (функции покупок, средний риск - делать по одной функции)

---

### ⏳ 10. Модуль "webapp/js/api/visits.js" (статистика и посещения) - **НЕ НАЧАТО**

- ⏳ `trackShopVisit(shopOwnerId, productId)` — отслеживание посещения магазина (~26 строк) - **НЕ НАЧАТО**
  - Используется в: `data.js`, `products_modal.js`
  - Приоритет: 🟢 НИЗКИЙ (отслеживание посещений, низкий риск)
  - Статус: Ожидает выполнения

- ⏳ `getVisitStatsAPI()` — получение статистики посещений (~16 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_stats.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в админке, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getVisitsListAPI(limit, offset)` — получение списка посещений (~17 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_stats.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в админке, средний риск)
  - Статус: Ожидает выполнения

- ⏳ `getProductViewStatsAPI(limit)` — получение статистики просмотров товаров (~17 строк) - **НЕ НАЧАТО**
  - Используется в: `admin_stats.js`
  - Приоритет: 🟡 СРЕДНИЙ (используется в админке, средний риск)
  - Статус: Ожидает выполнения

**Статус:** ⏳ **НЕ НАЧАТО**

**Приоритет:** 🟡 СРЕДНИЙ (функции статистики, средний риск - делать по одной функции)

---

## Итого:

- **Всего функций:** ~50+
- **Приблизительный объем кода для выгрузки:** ~1200+ строк
- **Текущий размер файла:** ~1290 строк
- **После удаления закомментированного кода останется:** ~50–100 строк (базовые утилиты, экспорты, импорты)
- **Созданные модули:**
  - ⏳ `webapp/js/api/config.js` - базовые константы и утилиты
  - ✅ `webapp/js/api/context.js` - контекст и настройки магазина (все функции выгружены)
  - ⏳ `webapp/js/api/categories.js` - категории
  - 🔄 `webapp/js/api/products_read.js` - чтение товаров (fetchProducts выгружена)
  - ⏳ `webapp/js/api/products_update.js` - обновление товаров
  - ⏳ `webapp/js/api/products_delete.js` - удаление товаров
  - ⏳ `webapp/js/api/reservations.js` - резервации
  - ⏳ `webapp/js/api/orders.js` - заказы
  - ⏳ `webapp/js/api/purchases.js` - покупки
  - ⏳ `webapp/js/api/visits.js` - статистика и посещения

---

## Рекомендуемый порядок выгрузки:

1. ⏳ **webapp/js/api/config.js** - НЕ НАЧАТО (STEP 1) - Базовые константы и утилиты
   - Приоритет: 🟢 НИЗКИЙ (базовые утилиты, низкий риск - можно делать рано)
   - Статус: Ожидает выполнения

2. ✅ **webapp/js/api/context.js** - ВЫПОЛНЕНО (STEP 2) - Контекст и настройки магазина
   - Приоритет: 🔴 ВЫСОКИЙ (критичные функции инициализации, высокий риск - делать осторожно!)
   - Статус: Все функции выгружены (getContext, getShopSettings, updateShopSettings)

3. 🔄 **webapp/js/api/categories.js** - В ПРОЦЕССЕ (STEP 3) - Категории
   - Приоритет: 🟡 СРЕДНИЙ (функция загрузки данных, средний риск)
   - Статус: Функция fetchCategories выгружена, старый код закомментирован, добавлен реэкспорт для обратной совместимости

4. 🔄 **webapp/js/api/products_read.js** - В ПРОЦЕССЕ (STEP 4) - Чтение товаров
   - Приоритет: 🟡 СРЕДНИЙ (функции чтения данных, средний риск)
   - Статус: Функция fetchProducts выгружена, старый код закомментирован, добавлен реэкспорт для обратной совместимости

5. ⏳ **webapp/js/api/products_update.js** - НЕ НАЧАТО (STEP 5) - Обновление товаров
   - Приоритет: 🟡 СРЕДНИЙ (функции обновления товаров, средний риск - делать по одной функции)
   - Статус: Ожидает выполнения

6. ⏳ **webapp/js/api/products_delete.js** - НЕ НАЧАТО (STEP 6) - Удаление товаров
   - Приоритет: 🟡 СРЕДНИЙ (функции удаления, средний риск - делать по одной функции)
   - Статус: Ожидает выполнения

7. ⏳ **webapp/js/api/reservations.js** - НЕ НАЧАТО (STEP 7) - Резервации
   - Приоритет: 🟡 СРЕДНИЙ (функции резерваций, средний риск - делать по одной функции)
   - Статус: Ожидает выполнения

8. ⏳ **webapp/js/api/orders.js** - НЕ НАЧАТО (STEP 8) - Заказы
   - Приоритет: 🟡 СРЕДНИЙ (функции заказов, средний риск - делать по одной функции)
   - Статус: Ожидает выполнения

9. ⏳ **webapp/js/api/purchases.js** - НЕ НАЧАТО (STEP 9) - Покупки
   - Приоритет: 🟡 СРЕДНИЙ (функции покупок, средний риск - делать по одной функции)
   - Статус: Ожидает выполнения

10. ⏳ **webapp/js/api/visits.js** - НЕ НАЧАТО (STEP 10) - Статистика и посещения
    - Приоритет: 🟡 СРЕДНИЙ (функции статистики, средний риск - делать по одной функции)
    - Статус: Ожидает выполнения

---

## Легенда статусов:

- ✅ **ВЫПОЛНЕНО** - Функция выгружена, протестирована, старый код удален
- 🔄 **В ПРОЦЕССЕ** - Частично выполнено, старый код закомментирован
- ⏳ **НЕ НАЧАТО** - Ожидает выполнения

---

## Примечания:

- Рефакторинг проводится мягко и постепенно, по одной функции
- Старый код сначала закомментирован для проверки работоспособности
- После подтверждения работоспособности (через 1-2 дня) старый код удаляется
- Каждый шаг тестируется отдельно перед переходом к следующему
- Критичные функции (getContext) рефакторятся осторожно
- Тесты в браузере не делаем - инициализация проходит через Telegram ID
- Все необходимые логи только в терминалах (console.log)

---

## Шаблон для каждого шага:

### Перед началом:
```javascript
// TODO: REFACTORING STEP X.Y - [Название шага]
// Дата начала: YYYY-MM-DD
// Статус: В процессе / Завершен / Откат
```

### В процессе рефакторинга:
```javascript
// ========== REFACTORING STEP X.Y: [Название] ==========
// НОВЫЙ КОД (используется сейчас)
import { newFunction } from './api/new_module.js';
const result = await newFunction(params);

// СТАРЫЙ КОД (закомментирован, будет удален после проверки)
/*
export async function oldFunction(params) {
    // старая реализация
}
*/
// ========== END REFACTORING STEP X.Y ==========
```

### После проверки (через 1-2 дня):
- Если все работает: удалить закомментированный код
- Если есть проблемы: откатить изменения

---

## Чеклист безопасности для каждого шага:

### Перед началом:
- [ ] Создан git commit с текущим состоянием
- [ ] Работа в отдельной ветке
- [ ] Прочитан план для этого шага
- [ ] Понятны зависимости функции (что она использует, где используется)
- [ ] Проверены импорты и экспорты

### После изменений:
- [ ] Проверено через Telegram Mini App
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] Нет ошибок в логах терминала
- [ ] Протестированы все сценарии использования функции
- [ ] Импорты корректны (относительные пути)
- [ ] Экспорты корректны (export function/const)
- [ ] Создан git commit с описанием

### Через 1-2 дня:
- [ ] Все еще работает корректно
- [ ] Удален закомментированный код
- [ ] Создан финальный commit

---

## Оценка времени:

- **Этап 1 (config.js):** 1-2 часа
- **Этап 2 (context.js):** 2-3 часа (критичные функции инициализации)
- **Этап 3 (categories.js):** 1 час (одна функция)
- **Этап 4 (products_read.js):** 1-2 часа (две функции)
- **Этап 5 (products_update.js):** 4-6 часов (8 функций, делать по одной)
- **Этап 6 (products_delete.js):** 2-3 часа (4 функции, делать по одной)
- **Этап 7 (reservations.js):** 2-3 часа (5 функций, делать по одной)
- **Этап 8 (orders.js):** 4-5 часов (10 функций, делать по одной)
- **Этап 9 (purchases.js):** 3-4 часа (7 функций, делать по одной)
- **Этап 10 (visits.js):** 1-2 часа (4 функции, делать по одной)

**Общее время:** 21-31 час работы

---

## Риски и митигация:

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Сломанная функциональность API | Средняя | Высокое | Сохранение старого кода, постепенный переход, тестирование каждого шага |
| Проблемы с импортами/экспортами | Средняя | Среднее | Проверка путей импорта, использование относительных путей, тестирование |
| Конфликты в git | Низкая | Среднее | Работа в отдельной ветке, частые коммиты |
| Потеря производительности | Низкая | Низкое | Профилирование после рефакторинга |
| Проблемы с авторизацией (getBaseHeaders) | Средняя | Высокое | Особое внимание к функциям авторизации, тестирование всех сценариев |
| Проблемы с инициализацией (getContext) | Средняя | Высокое | Особое внимание к getContext, тестирование инициализации приложения |
| Проблемы с глобальными переменными | Низкая | Среднее | Проверка зависимостей от глобальных переменных |

---

## Структура после рефакторинга:

```
webapp/js/
├── api.js                    # ~50-100 строк (только реэкспорты и импорты)
├── API_REFACTORING.md       # Этот файл
└── api/
    ├── config.js            # Базовые константы и утилиты (API_BASE, getBaseHeaders, getBaseHeadersNoAuth, fetchOptions)
    ├── context.js           # Контекст и настройки магазина (getContext, getShopSettings, updateShopSettings)
    ├── categories.js        # Категории (fetchCategories)
    ├── products_read.js     # Чтение товаров (fetchProducts, getSoldProductsAPI)
    ├── products_update.js   # Обновление товаров (toggleHotOffer, updateProductAPI, updateProductNameDescriptionAPI, updateProductQuantityAPI, updateProductMadeToOrderAPI, updateProductQuantityShowEnabledAPI, updateProductForSaleAPI, bulkUpdateAllProductsMadeToOrderAPI)
    ├── products_delete.js   # Удаление товаров (deleteProductAPI, markProductSoldAPI, deleteSoldProductAPI, deleteSoldProductsAPI)
    ├── reservations.js      # Резервации (fetchUserReservations, fetchReservationsHistory, createReservationAPI, cancelReservationAPI, clearReservationsHistoryAPI)
    ├── orders.js            # Заказы (createOrderAPI, getShopOrdersAPI, getUserUsernameAPI, getMyOrdersAPI, getOrdersHistoryAPI, completeOrderAPI, cancelOrderAPI, deleteOrderAPI, deleteOrdersAPI, clearOrdersHistoryAPI)
    ├── purchases.js         # Покупки (createPurchaseAPI, getMyPurchasesAPI, cancelPurchaseAPI, getPurchasesHistoryAPI, getAllPurchasesAPI, updatePurchaseStatusAPI, clearPurchasesHistoryAPI)
    └── visits.js            # Статистика и посещения (trackShopVisit, getVisitStatsAPI, getVisitsListAPI, getProductViewStatsAPI)
```

---

## Важные замечания:

1. **Импорты:** Все API функции должны импортировать необходимые зависимости из `api/config.js` (API_BASE, getBaseHeaders, getBaseHeadersNoAuth)

2. **Экспорты:** Функции, используемые в других модулях, должны быть экспортированы (`export function` или `export async function`)

3. **Реэкспорты:** В `api.js` должны быть реэкспорты всех функций для обратной совместимости:
   ```javascript
   export { getContext } from './api/context.js';
   export { fetchCategories } from './api/categories.js';
   // и т.д.
   ```

4. **Зависимости от telegram.js:** Все функции используют `getInitData()` и `requireTelegram()` из `telegram.js` - нужно убедиться, что импорты корректны

5. **Тестирование:** Каждый шаг должен быть протестирован через Telegram Mini App перед переходом к следующему

6. **Логирование:** Использовать `console.log()` для логирования в консоль браузера (не делаем тесты в браузере)

7. **Обработка ошибок:** Все функции имеют обработку ошибок - нужно убедиться, что она работает корректно после рефакторинга

8. **Обратная совместимость:** Реэкспорты в `api.js` обеспечивают обратную совместимость - существующие импорты продолжат работать

9. **Формат данных:** Некоторые функции работают с FormData (createPurchaseAPI) - нужно убедиться, что эта логика работает корректно после рефакторинга

10. **Массовые операции:** Некоторые функции выполняют массовые операции (bulkUpdateAllProductsMadeToOrderAPI, deleteOrdersAPI, deleteSoldProductsAPI) - нужно убедиться, что они работают корректно после рефакторинга

