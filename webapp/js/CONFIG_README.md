# 📋 Конфигурация API

## Файл: `config.js`

Этот файл содержит все настройки для подключения к API backend.

## Основные настройки

### API_BASE_URL

Базовый URL для всех API запросов. Обычно это ngrok URL.

**Как обновить:**
1. Запустите ngrok: `ngrok http 8000`
2. Скопируйте HTTPS URL (например: `https://xxxx-xx-xx-xx-xx.ngrok-free.app`)
3. Вставьте его в `config.js`:
   ```javascript
   export const API_BASE_URL = "https://xxxx-xx-xx-xx-xx.ngrok-free.app".trim();
   ```

### API_TIMEOUTS

Таймауты для различных типов запросов (в миллисекундах):

- `health: 5000` - Проверка доступности API
- `products: 10000` - Загрузка товаров
- `categories: 8000` - Загрузка категорий
- `context: 8000` - Загрузка контекста
- `default: 10000` - По умолчанию

### RETRY_CONFIG

Настройки повторных попыток для критических запросов:

- `maxAttempts: 3` - Максимальное количество попыток
- `delay: 1000` - Задержка между попытками (мс)
- `backoff: 1.5` - Множитель для экспоненциальной задержки

## Использование

Конфигурация автоматически загружается в `api.js`. Не нужно импортировать `config.js` напрямую - используйте функции из `api.js`.

## Переопределение через window

Для отладки можно переопределить API_BASE_URL через `window.API_BASE_URL`:

```javascript
window.API_BASE_URL = "https://your-custom-url.com";
```

Это работает только в development режиме.

## Важные замечания

1. **Всегда обновляйте API_BASE_URL** при изменении ngrok URL
2. **Не коммитьте** реальные ngrok URL в git (используйте .gitignore)
3. **Проверяйте доступность API** перед деплоем
4. **Используйте HTTPS** для всех URL (Telegram Mini App требует HTTPS)

